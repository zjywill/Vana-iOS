# Vana

iOS 26 SwiftUI 聊天 app:agent 通过工具调用读 Apple Health,对话式分析。整体计划见 PLAN.md(里程碑推进,一次一个可见改动)。

用户看到的名字是 **Vana**(`CFBundleDisplayName`、界面文案、图标),仓库、工程、target、scheme
和源码目录也都叫 Vana(仓库是 `Vana-iOS`,和 Android 那份 `Vana-Android` 并排)。bundle id 和
`KeychainStore` 里那个 Keychain service 都是 `com.pinapia.vana.ios`——Android 那边是
`com.pinapia.vana`,iOS 这一侧多一个 `.ios` 后缀,原因见下面那段。

**这两串从现在起不要再改。** 它们是已装设备上 API key 和已授权健康数据的钥匙——换掉就是把
用户存的 key 和给过的健康授权一次性作废,而屏幕上不会有任何一句话解释为什么。而且**首次上传
到 App Store Connect 之后 bundle id 就永久锁死了**,想改也改不了。

(2026-08-11 那次 HealthChat → Vana 的改名特意把这两串留在 `com.junyizhang.HealthChat`,
理由就是上面那条。2026-08-12 发第一个 TestFlight build 之前才改过来:那时候唯一装着这个 app
的是开发机,而换到新的付费团队之后 Keychain 的 access group 是 `TeamID.BundleID`,那把 key
无论改不改都读不回来——代价已经在付了,而那是最后一次能免费改的时刻。
后缀是那次改动踩出来的:先用了和 Android 一样的 `com.pinapia.vana`,归档时 Xcode 顺手把这个
App ID 注册到了当时那个团队(`TBZQNF7555`,另一个项目的组织账号)名下,ASC 里也建了记录。
**bundle id 在 Apple 那边全局唯一**,记录一旦存在 App ID 就删不掉——报的是
"appears to be in use by the App Store"——而已删除 app 的 bundle id Apple 又明写着不能重用。
所以换团队时连 bundle id 一起换了,不去赌 Apple 释不释放。)

Android 版在 `../Vana-Android`(Compose),包名 `com.pinapia.vana`。**两边共用这一份设计决策文档**:
那边的 `CLAUDE.md` 只记 Android 这一侧真正不一样的地方(Health Connect、minSdk、平台替换表),
其余一律回来读这里。

## 构建

`project.yml` 是工程唯一事实来源,增删文件后必须重新生成:

```bash
xcodegen
xcodebuild -project Vana.xcodeproj -scheme Vana \
  -destination 'platform=iOS Simulator,name=iPhone 17' build
```

## 测试

两套,都要过:

```bash
(cd AgentRuntime && swift test)   # agent core,秒级,不需要模拟器
xcodebuild -project Vana.xcodeproj -scheme Vana \
  -destination 'platform=iOS Simulator,name=iPhone 17' test
```

`VanaTests` 是 app 侧的 loop 集成测试:脚本化的假模型 + 假能力,但 `ChatViewModel`、
事件归约、预算、压缩、换模型迁移都是线上那一套。改 agent 循环相关的东西先看这里。

## 运行

模拟器安装运行用 iOS Simulator MCP 的 build/launch(bundle id `com.pinapia.vana.ios`),先 attach 让用户看到面板。

## 架构:通用 agent core

`AgentRuntime` 是本地 SwiftPM 包,**不 import 任何模型 SDK,也不认识 HealthKit**。它只认两个协议:

- `AgentModelClient`——「一个能估 token、能流式跑一轮的模型」。`AIKitModelClient` 是唯一实现。
- `CapabilityRegistry`——「一组 JSON Schema 加一个执行闭包」。`HealthTools.registry` 是唯一实现。

`AgentLoop` 在这两者之上跑工具循环。上下文管理是它最要紧的部分,从轻到重:

0. **单次工具输出的硬上限**(`ContextPolicy.maxToolOutputCharacters`,默认 6000 字符)——
   进上下文之前就截,按行边界截。后面几档都是亡羊补牢:一次调用返回几万字符的话,那一轮
   的原始输出在被压之前必须先原样发出去一次。`metadata` 不截,图表和原始行只给界面看。
1. **整段摘要**(`ModelSummarizer`)——估算跨过 `compactionThreshold`(默认预算的 80%)就叫
   模型把远处那段写成 artifact。**发请求之前**做,不是撞墙之后:撞墙时已经没有从容处理的
   余地了。artifact 通过 `.historyCompacted` 事件交回 app 存盘,下轮直接复用,不重算。
   第二次压缩走**增量更新**:`SummarizationPlan.previousSummary` 带上一版摘要,`spanText`
   只放这之后的新对话。压摘要的摘要会让最早那几轮很快只剩一句客套。
2. **逐轮压缩**(`TranscriptCompactor`)——把某一轮的原始工具输出换成它的摘要形态。总结失败
   时也退回这一档,绝不让用户这一句问不出去。
3. **丢最老的一轮**——前两档都不够时的最后手段。
4. 还塞不下就报 `contextWindowExceeded`,不偷偷发一个超长 prompt 出去。

第 1 档排在第 2 档前面是有意的:机械压缩免费但会铲平工具轨迹,模型摘要花钱但留得住结论。
省那一次调用不值得拿追问要用的数据去换。

**规划的开销是手机上的一等问题**,这段每个工具轮都要跑一遍,而且跑在用户已经在等的时候:

- 不需要压缩时(绝大多数会话)只估一次,和没有这套东西时一样贵。
- 需要压缩时才建 `CostModel` 按条记账,只在被改动的那条上重估。别改回「每压一条就全量重估
  一遍」——那是 O(n²),110 轮的会话实测 717ms,改完 86ms。
- 逐条计价走 `estimateMessageTokens`(不带工具面)。工具 schema 是每次请求算一遍的常量,
  跟着每条消息重新序列化一遍占掉三成耗时。
- 成本模型假设估算可加,这个假设**只用来挑压谁**。报给上层的 `estimatedPromptTokens` 和
  预算判断永远是估算器对整份 prompt 的结果;偏了就退回逐条重估兜底。

`VanaTests/ContextBudgetPerformanceTests` 盯着耗时和 `phys_footprint` 峰值,改这块先看它。

## 架构:hook(`AgentHook` / `AgentHookDispatcher`)

loop 生命周期上的旁观位,**第二条通道**,不是往事件流上加事件:

- 事件流(`AgentTurnEvent`)服务的是正在看这条回复的那块屏幕:每个字都要送到、顺序不能错、
  慢一帧就是界面卡。它的消费者只有一个。
- hook 服务的是不在场的那些消费者:埋点、统计、答完之后再花一次调用生成点什么。让它们去挤
  那条通道的代价是双份的——一个慢消费者就能拖住流式输出,而且每加一件事都要在
  `ChatViewModel.apply(event)` 那个界面状态机上开一个洞。

三个边界,一次 `run()` 一个 `turnId`:`turnStarted`(发第一次请求之前,带那一刻的历史快照)、
`toolFinished`(一次能力跑完,带名字/耗时/输出长度)、`turnFinished`(收尾)。

四条不要破坏的:

- **hook 改不了这一轮的任何东西**。`observe` 没有返回值,loop 也不读它的任何输出。想否决一次
  工具调用、想往 prompt 里塞一段,都**不走这儿**:那些决定发生在装配的时候(挂哪些能力、
  system 段拼什么,见 `allowsRecall` / `allowsMemoryWrites`),那时候还没有人在等。挪进 loop 就
  是让用户等一个外部决定,而且这一轮的预算已经估过了,账会当场对不上。
- **hook 永不阻塞 loop**。`post` 只入队。每个 hook 一条尾巴:同一个 hook 收到的通知严格按发生
  顺序,不同 hook 之间不保证顺序也不互相拖累(一个卡住十秒,别的照常收)。
- **hook 看不见 delta**,只看得见边界。思考模型一秒吐几十个 delta,给每个 delta 加一次 actor
  往返是纯亏,而它们本来就不需要:要内容的在 `turnFinished` 里拿整份 transcript,那份就是模型
  当时看到的原样。
- **收尾只有一个出口**。答完、轮数用光、用户按停止、救不回来的错误,全走 `turnFinished`,由
  `state` 分(轮数用光算 `.completed`,要区分的看 `finishReason.raw`)。少一条出口,hook 就得
  靠超时去猜自己在等的那一轮是不是已经不会来了。停止那条**在流结束之后才到**——它本来就不在
  用户等的那条路上。

**宿主由 app 持有**(`AgentHookDispatcher`),不由 loop 现造:hook 跨轮有状态(「上一轮答完了」
要被「下一轮开跑了」作废),而引擎每轮现造一个。宿主跟着造的话,两轮之间的顺序就没人保证了,
而那正是唯一要紧的一处顺序。后台派生的那几轮默认不挂 hook:没有用户在场。

### 第一个使用者:追问 chip(`FollowUpSuggestionHook`)

输入框上方那排 chip 原来是四句写死的话。现在第一颗仍然固定(「详细一点」对任何一段回答都成立,
而且它正是最想在模型还在写的时候点的那一句),后面三条由这个 hook 在答完之后生成
(`FollowUpSuggester`,一次不带工具的便宜调用,和首屏那三条同一套做法)。

- **兜底永远在,但不和生成的混排**。生成中、生成失败、没配 key 都退回固定那三条;生成成功但
  只凑出两条,就只显示那两条(位置空着)。混着排的话一半是贴着这段回答的话、一半是泛泛的套话,
  比少一颗更像坏了。门槛是**两条**不是三条:中文追问写到十一二个字很常见,按"恰好三条"收的话
  一条超长就把另外两条也带走,而那条路径是静默的——界面上只表现为"没生成"(第一版就是这么
  一直不出 chip 的,所以 `FollowUpSuggester` 里留了一处 DEBUG 日志打模型原文)。ViewModel 那边只有
  `followUps: [String]`,没有「正在生成」这种状态——多一个状态就多一个转圈,而它转的那两秒里
  用户什么都不缺。整排替换更糟:答完的那一刻 chip 那排先空一下再抖出来,恰好发生在他刚读完
  回答、正要往下问的时候。
- **材料分在两条通知里**:问句在 `turnStarted` 的历史里(这一轮的 transcript 还是空的),回答在
  `turnFinished` 里,靠 `turnId` 对上。认「最近那一条」会在插话续跑时拿上一轮的问句去配这一轮
  的回答。
- **工具只给名字不给输出**。查过什么说明结论建立在哪些数据上,决定了追问该往哪个方向问;带上
  数字只是让这一次生成多付一整段上下文的钱(同 `MemoryExtractor` 不喂工具输出)。
- **下一轮开跑就作废**。`turnStarted` 里取消在飞的那次生成:那几条接的是上一句,而屏幕上马上
  会多一段新的回答。插话续跑时这一下在几微秒内发生,基本没花钱。停掉的、报错的、助手一个字
  没说的那几轮不生成——用户此刻想按的是重试,不是追问。
- **hook 一条会话一个**,换会话时连宿主一起丢(它记着的是上一句问了什么)。上一条会话还在飞的
  那次生成由交付时的会话号挡住。

注入假引擎的那条路不挂 hook:hook 的行为由 `VanaTests/FollowUpChipTests` 直接对着
`AgentLoop` 验,不必穿过 `ChatViewModel` 那个状态机。

## 架构:首屏那段话(`HealthSituation` / `HealthVitals` / `QuickSummaryWriter` / `HealthStatusView`)

打开 app 看到的第一段字说**现在是什么状况、要不要在意**,而不是先问用户一个问题。在这之前
首屏只有三个问句——而那几个问题的答案本地早就算出来了(`HealthSituation.detect()`),用户却
要点一下、再等模型联网查一遍 HealthKit,才听到和本地那句同一个意思的第一段话。

**材料是现成的,加的只是一个位置。** `HealthTrigger.brief` 本来就是"昨晚只睡了 6.2 小时,
比最近常态少 100 分钟"这种句子,原来只喂给模型和通知。所以这是 `detect()` 的**第四个消费
者**(check-in 通知、首屏那三条、Siri 的 `SpokenBrief`),必须走同一次判定——同一份数据在
四个地方说出四种结论,用户只会觉得这 app 自己都没想清楚。

### 没有波动不等于没有现状(`HealthVitals`)

**第一版最大的一处错**:那段话只由触发点写成,于是数据平稳的日子里——多数人多数天——它退化
成一句「最近几天没有读到值得特别留意的波动」,一个数字都没有。用户读完第一行字,反而不知道
自己现在怎么样。触发点回答的是**「有什么变了」**,而他打开 app 想知道的第一件事是
**「现在是多少」**。

`HealthVitals` 补的就是后者:昨晚睡眠、今天步数、静息心率、HRV、最近一次锻炼、体重,每项带
一句和常态的对比。**材料白拿**——`detect()` 本来就把这五份数据查了一遍,判完触发点就扔了,
所以这里一次 HealthKit 查询都没有多加。

- **有触发点时仍然是触发点开头**,现状排在后面。那才是他此刻打开 app 的原因。
- **读不到的项目不进句子、不进 prompt,但留在详情页里**(带「昨晚没有记录」)。列表上直接
  不显示的话,用户只会以为 app 没看那一项——而「昨晚没戴表」和「昨晚睡得挺好」差得很远。
- **「从来没记过步」和「今天 0 步」是两件事**。两周一步没有说明这台设备没在记步,
  「0 步（最近平常 0 步）」是纯噪声;而有基线时的今天 0 步是真的有话要说(`.noStepsToday`)。
- **步数只报常态,不判偏多偏少**。它是攒了一天的量:上午八点两千步远低于日均,那是时间还早。
- **噪声大小的差别说成「差不多」,不报那个数**。静息心率日间本来就晃一两次,把「比基线高
  1 次」写进去,模型会认真解释一个纯噪声。
- 一个读数都拿不到时那句 `calmSummary` 说的**不是「他很平稳」**,而是这台设备上什么都没
  读到(没授权、刚装上、没戴表)。更不写「一切正常」。

### 卡片只露前几行,点开是详情页(`HealthStatusView`)

那段话现在要先把现状说清楚,本来就写得比一句长(上限 160 字);而这张卡排在欢迎卡前面,
占满一屏就把下面的话题和建议全推走了。所以卡片 `lineLimit(3)` 截断,**读全的地方在详情页**
——顺带,那一页才有位置放「这话是根据哪几个读数说的」。

于是它**成了按钮**(原来明确不是)。改的理由是这一下通向的是同一件事的下一层(整段话 +
读数),不是下面那几颗 chip 的第二个入口:那几颗是去提问,这一颗是往下看。详情页里因此也
**没有可点的问题**——那才会变成同一屏里两个控件干同一件事。

家人成员那条路上 `situation` 是 nil(`HealthSituation` 整个不跑,那份数据属于机主),卡片退回
一张不可点的卡。

### 那颗刷新按钮

生成一次启动只跑一次,那是省钱的默认;但**用户对写出来的那段不满意**是一种没有出口的处境:
数据是对的,话没说到点上,而他能做的只有关掉 app 再打开。给一颗按钮就够了——他自己按的,
不是每回到首屏就自动花一次钱。它同时重查一遍数据:按它的另一半理由是「我刚同步完手表」,
**那一半不需要模型,所以没配 key 时这颗按钮照样有用**——第一版在那儿直接 `return` 了,
表现就是"点刷新没反应"。

**按完必须说一句话。** 数据没变时那一下是完全无声的:句子一模一样、读数一模一样、按钮转一下
就停,用户只会当它坏了。所以那段话底下那行小字按状态说清四件事(正在写 / 已重新读取 /
根据这些读数写的 / **还没配置云端模型,这段话是本机拼的**),最后那句尤其要有:不说的话,
没配 key 的人手里这颗按钮永远不会改变任何东西,而屏幕上没有一个字解释为什么。

### 其余几条不要破坏的

- **和 `suggestions` 同一套两级**:本地那句立刻出(零延迟、不花钱、没配 key 也有),模型写的
  那段回来了原地淡入换掉。两次调用**并发发**,各自失败各自兜底。
- **模型那段是流式的**。非流式的一次调用在刷新按钮按下去之后是十几秒的空白,而这段话本来
  就是一句一句成形的。收尾才校验(`parse`),流式期间只剥壳(`partial`)。
- **只取最前面两条触发点、最多三项现状**。再往下就是把首屏写成一份日报;剩下那几条本来就会
  以问题的形式出现在下面那三颗 chip 上(`questions` 取的是同一个序列),完整的一份在详情页。
- **`QuickSummaryWriter` 只喂结论,一行原始数据都不给**(和 `QuestionSuggester` 的唯一区别,
  有测试盯着)。进去的是「昨晚睡眠 7.2 小时（比最近 6 晚平均多 18 分钟）」这种判好的一行,
  不是十四天的逐日样本。那边要写具体的问题,得看见原始数字;这段话不用。多给的每一个数字都
  是它可以写错的数字,而这是用户读到的第一段——帖子里抱怨 Gemini Health "偶尔报错健康数据"
  正是这条路上出的事。
- **一个读数、一个触发点都没有时才不叫模型**(`hasSummaryFacts`)。让它为"什么都没读到"写
  一句,它会为了有话说而开始编。反过来,**只有现状没有触发点时要叫**——那正是它该说
  「这几项都在你最近的常态里」的时候,而 prompt 里明写了一句「数据里没有读到值得特别留意的
  波动」,不说的话模型会去猜这几行里哪个算异常。
- **写超了整段作废,退回本地那句,不截断**。一段话在"要不要在意"之前被切断,剩下的正好是最
  没用的那半段。作废这条路是静默的(界面上只表现为"本地那句一直没换掉"),所以留了一处 DEBUG
  日志打模型原文,同 `FollowUpSuggester`。

`VanaTests/QuickSummaryTests` 和 `HealthVitalsTests` 盯着这一套。

## 架构:失败处理

同样分档,越靠外越少见。原则是**能自愈的不要惊动用户,惊动了就要说人话**:

- **工具失败** → 变成一条 `isError` 的结果继续跑。模型自己会换个问法。
- **模型失败** → `ModelFailure` 分类,分两条通道:传输层看 `NSURLErrorDomain` 的错误码
  (`localizedDescription` 是跟着系统语言走的,中文手机上永远匹配不上 "timed out");
  provider 侧看返回的原文(那段不本地化,而且经过 SDK、网关转手之后只剩它)。
  - 拥塞、限流、网络抖动 → 指数退避重试(`RetryPolicy`,默认 3 次)。重跑之前先发一个
    `.textRolledBack` 把已经吐出去的半句撤掉,否则半句会和重试的整句连在一起。撤字和
    `.retryScheduled`(告诉用户在重试)是两个事件:重跑不一定是重试,压缩后重跑也要撤字。
  - 额度、鉴权、模型不存在 → 立刻上报。对着「key 不对」重试三次只是把同一句话说三遍,
    还把真正的原因往后拖了十几秒。
  - 上下文超限 → 不重试(原样再发还是塞不下):强制总结一次 + 把校准比例放大 25%
    (provider 说估小了就是估小了)+ 水位线降到 `thresholdAfterOverflow`,然后重跑这一轮。
    只做一次;再超就报 `contextWindowExceeded`,让用户去开新对话。
- **模型在吐 tool_use 的中途被输出上限截断**(`finishReason == .length` 且有 pendingCalls)
  → 这一批**一个都不执行**,每个都回一条 error 结果让模型重发。JSON 能抢救出来不代表参数
  完整,拿半截日期去查会查出一组看着很正常但是错的数字。
- **工具轮数用光** → 不是错误。照常 `turnCompleted`,finish reason 记 `toolRoundLimitReason`,
  已经查到的东西全部留着。把三次查询结果丢掉去报一个「查询次数过多」对用户是净损失。

压缩本身也发事件(`.compactionStarted` / `.compactionFailed`)。静默降级等于线上出问题时
无从查起。

最近 `preservedRecentTurns`(默认 2)轮在第 1、2 档里受保护:刚查完的数据被压成一句摘要,
用户下一句「那第三天呢」就答不上来了。只有真超预算才动它们。

`ContextCalibration` 按 provider+model 归档本地估算和实际计费的比值,换模型即作废。取最近
几次的**中位数**,离谱的样本直接丢:命中缓存的那一轮 provider 可能只报没走缓存的部分,只信
最近一次的话下一轮就拿这把废尺子去量。

几条不要破坏的边界:

- **换模型本身不是丢历史的理由**。`whenWindowShrinks` 只决定「先压谁」(从大窗口带过来的
  那几轮排前面)和把阈值降到 `thresholdAfterModelSwitch`,压不压由预算说了算。之前那版
  换个模型就把所有工具轨迹铲平,哪怕整段对话只有两轮——白丢了追问要用的数据。

- 会话文件里不能出现 AIKit 类型。transcript 存 `AgentTranscript`,旧格式在 `ChatMessage`
  的 decoder 里翻译一次(有测试盯着)。
- compaction artifact 分两份读者:`visibleSummary` 给用户,`replaySummary` 给模型(多带一段
  工具轨迹)。不要退化成「只回放可见文本」。
- app 写给用户的占位文案("已停止回复")打 `textIsPlaceholder`,runtime 靠这个标记判断要不要
  回放——不要让 runtime 去比对某句中文。

- **assistant 的 `.reasoning` 是数据,不是日志**。DeepSeek 的思考模型规定:带 `tools` 的请求
  必须把每一条中间 assistant 的 `reasoning_content` 原样发回去,少一条就是 400。所以
  transcript 里的 `.reasoning` 不能在存盘、回放、迁移的任何一环被顺手删掉。发不发由 AIKit
  按 `ModelInfo.interleaved.field` / dialect 决定——反过来对着一个没听过这字段的 provider
  发它,同样是 400。

## 架构:回复期间接着说话(排队与插话)

等上一句答完才能开口,是这个 app 里最不像跟人说话的一处:模型跑六轮工具要十几秒,而人在
这十几秒里想起来的那句「顺便也看看心率」根本没地方说。所以 `send()` 不再被 `isReplying`
拦住——打出去的字立刻变成气泡,由 `AgentLoop` 在**下一个工具轮边界**接进上下文。

**打断发生在轮边界,不在流中途**。中途打断要撤掉已经吐出去的半句(用户正读着的那段字会
当场消失),还得把刚花掉的那次生成整个扔了;而边界处什么都不用撤,模型下一句就能改道。
真要立刻停下,停止按钮一直在原位。

**排队和已送达必须在界面上分得开**(`ChatMessage.isQueued`)。两种状态是同一个气泡,而猜错
的那个方向恰好是最糟的——以为说过了,其实没说。排队中的气泡淡一档,底下补一句
「Vana 还没看到」;被取走的那一刻翻面。不写「发送中」:那会让人以为只是慢一点,而它可能
一直排到这一轮结束。

几条不要破坏的边界:

- **接的位置在 `runtimeTranscript` 尾部,不在 `history` 里**。它到得比这一轮已经发生的工具
  调用还晚;放进 history 会让模型读成「他先追问、我才去查」,而实际顺序正相反。
- **插话被接走的那一刻,这一轮的回复要在界面上劈开**(`splitReplyAroundInterjection`),
  后半段另起一条排在插话下面。不劈开的话,答这句话的正是它**上面**那条还在写的回复——
  用户看到的是「我问了,它没理我」,而模型其实早就答了(第一版就是这么翻的车)。根子在于
  消息列表是线性的,而一条跨过插话的回复在时间上压着它,没有哪个位置是对的;劈开之后
  每一段都落回它该在的位置。前半段一个字没说也没查东西时直接扔掉,那只是个空气泡。
  **劈开只发生在界面上**:runtime 那边前后半段仍然是同一轮,整轮的 transcript 到最后
  一次性落在后半段上。所以前半段的 id 也要进后半段的 `inlinedMessageIDs`,否则它会被
  `reconstructedReplayMessages` 重建出来,同一次查询在 prompt 里出现两遍。
- **返回即消费**(`AgentPendingInputProvider`)。loop 拿到就会发出去,取的那一步必须同时清掉
  队列标记,否则下一个边界会把同一句再发一遍。
- **事件的收件人是 `replyingMessageID`,不是 `messages.last`**。插话排在正在写的那条回复
  **后面**,照旧取最后一条的话,模型吐出来的字会一个个落进用户刚打的那句话里。这条不报错,
  只是屏幕上出现一句谁也没说过的话。
- **插话和前半段都在两个地方各存了一份**:消息列表里各一条气泡(界面要显示),那一轮的
  `exactTranscript` 里也各有一段(模型当时就是这么读的)。回放时靠
  `StoredAgentTurn.inlinedMessageIDs` 跳过列表上那两条,和 compaction 的
  `absorbedMessageIDs` 同一套机制。**但要先确认 transcript 真的存下来了**——那一轮被停掉时
  它是空的,回放走的是从工具记录重建的那份,里面既没有插话也没有前半段;这时候还跳过,
  用户补的那句就凭空消失了。
- **最后一次请求之后才到的那句,由 app 接着跑一轮**(`startReply` 的 while)。loop 已经没有
  边界可以接它了,不续跑的话它就一直排在那儿:屏幕上是一条发出去了的消息,而模型永远不会
  回应它。
- **按了停止不续跑,队列也不清空**。他按停止的意思就是别再发了;但打的字不能替他扔掉,
  排队状态跟着会话存盘,发送按钮一直亮着等他决定。
- **发送按钮做什么只看输入框里有没有字**。以前是「正在回复就是停止」,因为那时候根本发不
  出去;现在手指刚敲完一句话、按下去却把上一条答到一半的回复掐了,是最容易惹恼人的一个
  误触。输入框空着时它还是停止,够得着。
- 收回只对排队中的那几条开放。已经发出去的那句模型已经看过了,从列表里抹掉它只会让屏幕上
  的对话和模型记得的对话对不上。

系统提示里那句「用户可能中途补一句」只在前台会话发(`acceptsInterjections`)。不说这一句,
模型多半会把插话当成一个全新的问题,从头把刚说过的再讲一遍——而用户补那一句的意思恰恰是
「别那样」。后台派生的那几轮没有用户在场,那段话对它们只是白占 token。

一处已知的 provider 依赖:插话会紧跟在工具结果后面,对 Anthropic 那类把 tool_result 也算作
user turn 的 provider,就是连着两条 user 消息。Anthropic 明确会合并,OpenAI、DeepSeek 也收。
真碰到严格要求交替的 provider,该在 AIKit 的 prompt 转换那一层合并,不要退回「不许插话」。

`VanaTests/QueuedMessageTests` 和 `AgentRuntimeTests/PendingInputTests` 盯着这一套。

## 输入区上方那颗「回到底部」(`ChatView.jumpToBottomButton`)

往回翻旧消息之后,回到最新一条原来只能一路手动滑下来——而这一屏的消息是非 Lazy 的整列,
一段长对话滑到底要好几下。翻远了(离底 240 点以上)就在输入区正上方浮出一颗箭头。

- **出现的条件是「离底多远」,不是「滚了没有」**(`onScrollGeometryChange` 只取一个 Bool)。
  把偏移量搬进 `@State` 的话,手指一路滑下来就是每帧一次界面刷新,而这一屏每次刷新要重画
  整列气泡。门槛定在一条长回复的高度:再近他自己一划就到了,而按钮反倒挡着字。
- **「下面还剩多少」只有一种算法**(`ScrollBottomDistance`):
  `contentSize.height - visibleRect.maxY + contentInsets.bottom`。**别自己拿 offset 去凑**——
  `containerSize` 已经是**扣掉安全区之后**的高度(实测 874 − 116 − 156 = 602),而 `visibleRect`
  是含安全区的那一份。第一版写成 `contentSize + insB - (offset + containerSize)`,等于把底部
  安全区算了两遍:贴底时算出 284、门槛才 240,那颗按钮于是**一直亮着**——界面上不报错,只是
  一个本该藏起来的按钮从不消失,真机上才被逮到。两组实测值钉在
  `VanaTests/JumpToBottomTests` 里(贴底 11;往回翻 708 点后 719)。
- **它不进输入区那一列。** 做成 `VStack` 里的一行,每次出现和消失都会把整段对话顶一下——
  偏偏就在他正读着旧消息的时候。挂在 `ScrollView` 的 `overlay` 上,底部留出输入区的高度
  (`onGeometryChange` 现量,chip 那排、附件、波形都会让它长高)。
- **不做成输入区的 `overlay` 往上偏移。** 画得出来,但**点不着**:画到父视图框外的那部分
  收不到触摸,表现是按钮好端端地摆在那儿、按下去什么都不发生(在 iPad 上试过一次,
  确认过它把触摸放给了下面那颗话题 chip)。
- **贴底仍然只有 `scroll(with:animated:)` 一个入口**,所以 `safeAreaInset` 跟着挪进
  `ScrollViewReader` 里面。为这颗按钮另起一套滚动机制(`ScrollPosition`),就是这一屏
  最早那阵抖动的来路。
- 画出来 36、点得着 44。空会话不出——那一屏本来就没有「底部」可回。

## 架构:回答里的 markdown(`MarkdownBlocks` / `MarkdownTextView`)

助手气泡原来整段走 `AttributedString(markdown:)` 的 `.inlineOnlyPreservingWhitespace`——粗体、
代码这些**行内**语法认得,块级语法一个都不认。而模型很爱用表格列每日数据,于是屏幕上出现的是
一堆竖线和横杠。这不是"样式差一点",是**把一份整理好的数据退回成了原始文本**,而看不懂那堆
符号的人恰好最需要那张表(用户里有相当一部分是把系统字号调大了的老人)。

**修的是渲染,不是提示词。** 让模型别用表格也能消掉这堆符号,但每日数据本来就是表格形状——
按天扫一列数字正是它存在的理由,改写成散句是更难读。模型这里做对了,该改的是这一侧。

- **`.inlineOnlyPreservingWhitespace` 不能换成 `.full`。** `.full` 会按 CommonMark 把单换行
  折叠掉,模型列的每日数据糊成一坨(「22:26–05:19」直接粘上下一行的日期,踩过)。所以块级
  那一层在 `MarkdownBlocks` 里自己拆,行内那一层仍然交给系统。
- **必须有分隔行才算表格。** 只看竖线的话,正文里「深睡 | 核心 | REM」这种顿号写法会被当成
  一张两行的表——而它只是一句话。`---` 单独一行是分割线,不是分隔行。
- **不丢格子。** 某一行比表头多一格或少一格时补齐到最宽的那一行,不砍。这是健康数据,少显示
  一个数字比排版难看严重得多。
- **只有表头没有数据行是流式中途的正常状态**,不是错误:表头和分隔行先到,第一行还在路上。
- **宽度不够时换排法,不是换滚法。** 横向滚动最好写,但一张滚出屏幕的表,第一列(日期)一滑
  就看不见,剩下的数字失去参照;而"这里还能往右滑"本身要人先发现。放不下就把每一行摊成一小段
  (第一格当小标题,其余「表头 值」逐行列)——竖着变长在手机上是免费的。
- **用 `ViewThatFits` 判,不是"超过三列就摊开"。** 列数不是判据,**放不放得下**才是:同样三列,
  默认字号下宽宽松松,辅助功能的特大字号下一列都塞不进。配套那句 `.lineLimit(1)` 是必需的——
  少了它,挤成三行的格子也算"放得下"。
- **用户那一侧原样显示,不解析。** 他打的字不是 markdown,把「1*2*3」当成斜体是在替他改写他
  自己说过的话。

`VanaTests/MarkdownTests` 盯着这一套。

## 架构:记忆(`Vana/Memory`)

「关于这位用户」的长期记忆。`MemoryStore`(actor,`Documents/memory.json`)存,
`MemoryExtractor`(后台抽)和 `MemoryTools`(对话里当场记)写,
`MemorySnapshot.instructionBlock` 拼进 system 段,`MemoryView` 给用户改。

**没有索引层,也不要加**。Claude Code 那种「MEMORY.md 索引 + 按需读」是为了几百条记忆装不进
上下文而存在的,代价是漏召回——索引说有、模型没去读。这里的记忆对象只有一个人,全部加起来
几十条、一两千 token,全量进 system 段最简单也最可靠,上限(40 条 / 2000 字)就够了。

三条不要破坏的边界:

- **记忆里不存 HealthKit 查得到的数字**。存「昨晚睡了 6.2 小时」明天就是错的,存「他觉得
  睡够 7 小时才算好」永远对。这条不是靠提示词维持的:`MemoryExtractor.transcript(of:)`
  **只喂用户和助手说过的话,工具输出一条都不给**——它无从记起。别为了「让抽取器看到结论」
  把工具结果加回去。
  这条同时消掉了失效问题:不存易腐的东西就不用做 TTL。唯一带时限的是 `followUp`。
- **快照绑在会话上,会话之内不换**(`ChatViewModel.memory` → `AIKitEngine(memory:)`)。理由
  和「系统提示里的今天只精确到天」一样:system 段中途变化打掉 prompt 缓存,也让模型对用户的
  认知在一条对话里跳变。引擎不许自己去读盘。抽取写在会话结束、快照读在会话开始,正好配套。
- **用户主动写下或说出的(`origin != .extracted`,即 `pinned`)不许被抽取器动**,淘汰时也永远
  跳过。他改成那样就是不认同模型的说法,后台跑一遍又改回去,等于那个设置页是假的。
  `origin` 三态(`manual` 设置页手写 / `asked` 对话里让 Vana 记 / `extracted` 后台抽)是
  存下来的一等字段,界面每条都要说清「这句哪来的」。

写入有两条路,**互补,不是替代**:

- **`remember` 工具**(`MemoryTools`)——用户明说「记住我不能跑步」时当场落,气泡上出现
  「记住了「…」」。只留后台抽取的话,他关掉 app 之前无从知道到底记没记住。
- **会话结束抽取**(`MemoryExtractor`)——多数该记的事用户根本不会明说,而让模型每轮都去
  调一次 `remember` 是让用户多等一个往返。

只留一条都不行,所以两条都在。记忆关掉时 `remember` 连挂都不挂出去
(`CapabilityRegistry.healthChat(allowsMemoryWrites:)`):给一个只会报错的工具,模型得先调一次
才知道不行。**隐私会话走同一个开关**——两条写入路径必须一起堵,只挡抽取的话,模型在对话中途
调一次 `remember` 就把「不保存」变成了假话。工具不挂出去,system 段里那段「该记就调 remember」
也跟着不发:它本来就是照着 registry 里有没有这个工具拼的。

其余几处判断:

- 抽取跑在**会话结束**(切会话 / 退到后台),不是每一轮结束——每问一句多付一次调用,而多数
  健康对话三五轮就完了。门槛在 `MemoryHarvest.shouldHarvest`:隐私会话永不抽(说好不存就是
  不存),一问一答不抽,`memoryHarvestedMessageCount` 之后没有新消息不抽。**删掉的会话也
  不抽**——用户刚把这段对话删了,再从里面记下点什么是反着来的。
- **`followUp` 到期不等于删除**。到期那一刻正是它要派上用场的时候:进早上那条 check-in
  (`CheckInScheduler.content` 里排在所有触发点前面——那是用户自己定下的约定,触发点只是数据
  里冒出来的现象),进 system 段时标上「说好的时间已经到了」。点开通知就把那条删掉,否则
  接下来几天的早上会重复同一句。真正消失要再过 `MemoryStore.followUpGrace`(3 天)。
- 抽取器输出的是**操作**(add / update / delete),不是新的一份记忆全文。只会 append 的话
  三个月后有四十条「关心睡眠」;每次全量重写又会像「摘要的摘要」一样把最早那几条磨平。
  和 `SummarizationPlan.previousSummary` 同一个道理。
- 给模型看的是短编号(M1、M2…)不是 UUID:省 token,也因为模型抄长串容易抄错一位,抄错就是
  一条改不动的记忆。`MemoryStore` 的排序必须稳定,否则编号会指到别处去。
- 记忆块末尾那句「任何具体数值一律以本次工具返回的为准」是这套东西最要紧的防线,有测试盯着。
  没有它,模型会拿三个月前记下的一句话当今天的数据讲。
- 抽取全程失败即放弃。记忆学不到东西是小事,让保存会话跟着失败是大事。
- **app 侧的测试跑在 app host 里,`MemoryStore.shared` 就是模拟器上那份真的
  `memory.json`**。测试必须走 `MemoryStore(directory:)` / `MemoryTools.registry(store:)` /
  `ChatViewModel(memoryStore:)` 传自己的临时 store——这条踩过一次,一次 `swift test` 把用户
  记住的东西全删了。`SessionStore(parent:)` 和 `ChatViewModel(sessionStore:)` 同理——
  `loadsPersistedSession: false` 只挡住了**写**,而延续线和召回都要去读盘。

**「他总在问什么」不让模型抽**(`InterestProfile`)。那是统计:本地按会话数工具调用,越近的
会话权重越高,一条会话里同一个工具查五次只算一次(那是一个问题拆成五步,不是关心程度是别人
的五倍)。数工具调用而不是问句关键词——「今天不想聊睡眠」也含「睡眠」。模型抽的是他说过的
话,app 数的是他点过的东西,为数数花一次模型调用是纯亏。

兴趣**只做同级裁决**(`HealthSituation.ordered`),不做加权:刚练完永远排在体重变化前面,
哪怕他半年没问过锻炼。做成能翻盘的权重,第一条建议就会开始答非所问。接在 `detect()` 的排序
里,首屏建议、check-in、Siri 三处一起受益——同一份数据在三个地方说出三种结论,用户只会觉得
这 app 自己都没想清楚。

## 架构:用药与补剂(`Vana/Medications`)

一张回答**「我和这些东西是什么关系」**的表:在吃什么、需要时吃什么、试过什么没用、什么不能吃。
设计与任务拆分见 `MEDICATIONS.md`。

**它不是用药打卡本。** Apple Health 从 iOS 26 起有完整的用药追踪(排程、提醒、打卡、依从性),
HealthKit 也把它开放给第三方**只读**(`HKUserAnnotatedMedicationQuery` / `HKMedicationDoseEvent`,
两者的 `init` 都是 `NS_UNAVAILABLE`)。那套回答的是「我今天该吃什么、吃了没」;这张表回答的是
另一件它永远拿不到的事——`HKMedicationDoseEvent` 里没有一个字段叫「吃了没感觉」。这个功能的
产生场景本来就是一次问答的结果(「我头疼吃什么」「冬天要不要补点什么」),要记的是那次判断和
后来的结果。所以最值钱的一列是 `outcome`,不是剂量。**第一版不接 Apple Health**,只留
`healthConceptId` 和 `origin == .health` 两个空位。

形状上它最接近**记忆**,不接近健康数据:用户自己维护、几个月不变、他能改。所以边界抄记忆那套
(`origin` 三态、快照绑会话、末尾一句防线、followUp 回访)。不合并进 `MemoryItem` 的四条理由:
一条要有结构(名字/状态/情况/效果)、记忆那 40 条的池子会被十种补剂挤爆、记忆的淘汰机制对
「我对青霉素过敏」是致命的、用户要一个独立入口去看和改它。

### 名单进 system 段,细节走工具

**这是最要紧的一处判断。** 用药**不做触发式挂载**(和 `search_sessions` 正相反),因为漏的代价
不对称:召回漏了,用户补一句「上次不是说」就回来了;这里漏一次,是模型在不知道他吃着 β 阻滞剂
的情况下解释他的静息心率,或者推荐了一个他过敏的东西。要模型自己想起来去查,它想不起来的那次
恰好就是最该说的那次。

`MedicationSnapshot.instructionBlock` 每条一行,分组按 `MedicationStatus.allCases`
(**不能吃永远第一**),上限 24 条 / 800 字,**超了先裁「试过了」,`cannotTake` 一条都不裁**。
分组在 `instructionBlock` 里重排一遍,不指望传进来的顺序——快照是个值类型,谁都能造一个,
而排错的后果是禁忌那条掉到第三行去。

末尾那三句**是这个功能真正的产出,不是免责声明**,有测试盯着:
「他明确不能吃的绝对不要提」(安全)、「他试过没用的不要再推荐一次」(这个 app 在不在听)、
「剂量一律不给建议」(安全线)。少一句它就退化成一个记事本。

三个工具:`list_medications`(读全量,含被裁掉的和停掉的)、`log_medication`(当场落一条)、
`update_medication`(更新结果和状态)。**写是主路径不是辅路径**——记忆的多数条目靠会话结束后
抽取补上,而这张表的每一条都产生自一次明确的问答,用户当场就该看见它落下了。
按**名字**匹配不发 `M1/M2` 短编号:药名本来就短且唯一,用户也是直接说名字的;匹配不上就把现有
名字列出来,只回一句「没找到」的话模型只能瞎猜下一个写法。

### 回访是闭环

`followUpAt` 到期进早上那条 check-in(排在记忆的待跟进后面、目标进展前面——同是一个带日子的
约定)。**没有它 `outcome` 那一列永远是空的**:用户试了两周之后不会主动回来告诉 app 结果,
而那一列正是这张表最值钱的东西。

不替他先跑一轮(不像 `FollowUpRunner`):「褪黑素有没有用」**数据里没有答案**,替他查一遍睡眠
再说「你的深睡多了 12 分钟」,是拿一个碰巧的数字替他回答一个主观问题。

点开通知**只清掉约定,不删那条记录**——那样东西他还在吃,要走的只是「回头问一句」这件事。
从详情页那颗按钮进来的不走这条路,那不是在兑现约定。

### 其余几处边界

- **入口是 toolbar leading 第二颗 `pills`**,和会话列表并排,以 sheet 呈现(详情页那颗
  「问问 Vana」要回到聊天界面,而从 push 出来的两层里退回根视图没有干净的写法)。不放设置页:
  记忆一年改两次可以藏,这张表用户会反复打开看「我上次试的那个叫啥来着」。也不两处都放。
- **这张 sheet 里的 `NavigationStack` 中不要用 `withAnimation`**,动画一律用
  `.animation(_:value:)` 挂在会动的那个视图上。全局 transaction 会把整个更新扫一遍,
  闭包式 destination 的 `NavigationLink` 在那一下会被丢掉——表现是点「重新生成」当场弹回
  列表页。踩过一次(`MedicationDetailView.regenerate`)。
- **`MemoryExtractor` 不许抽用药**,提示词里明写。它最爱抽「我在吃他汀」这种 profile 记忆,
  抽了就是同一件事的两份,一份在设置里改、另一份在用药页改,而对不上的那次可能是禁忌那一条。
  同理 `remember` 的指令里也加了一句让路。**这条最容易漏,有测试。**
- **他自己的评价永远排在自动生成的一般说明前面**(列表副标题、详情页、工具输出三处)。
  一般说明模型随时能重写一遍,「我试了两周没感觉」只有他知道。反过来排,这张表读起来就像
  一份说明书摘抄,而那不是它存在的理由。
- **开关不归在 `memoryEnabled` 下面**(`EngineSettings.medicationsEnabled`)。关掉记忆的人
  不指望 Vana 还记得他随口说过的话,但仍然会指望这张他一条条录进去的表还在。关掉之后
  system 段不带名单、三个工具都不挂,**列表页照常能看能改**。
- **隐私会话照样读,不挂三个写工具**(同记忆)。禁忌那一组在隐私会话里尤其不能关掉:
  承诺的是不留痕迹,不是不管他死活。
- **store 不做淘汰**。`MemoryStore` 挤掉最久没更新的自动记忆是因为那些是模型猜的;这里每一条
  都是用户自己录的。装不下 prompt 是 `MedicationSnapshot` 的事(它裁的是进模型那一份,盘上
  一条不少)。文件用 `.completeFileProtection`,比 `memory.json` 严一档。
- **以某一条为话题**走 `SessionThread.medication(UUID)`,和目标线同构(21 天 idle,
  `isLongRunning`)。同样**不给话题**——今天问副作用、明天问要不要继续,写死在 system 段里
  第三天就对不上了。那颗按钮不预填问题,空会话那三条按状态本地拼(`openingQuestions`)。

**故意不做的两件**:不做提醒和打卡(Apple Health 已经做了,做第二套只会两边对不上,而漏一次
提醒的代价在这里是真的——空态那句话必须说清);不做本地交互作用数据库(那是给一个 Vana 兜不住
的结论背书,模型按一般知识说注意点、把结论指向药师,是这里唯一诚实的形态)。

`VanaTests/MedicationTests` 盯着这一套。

## 架构:家庭成员(`Vana/Tenant`)

一个成员一份完全隔离的数据。界面上叫「成员」,代码里叫 `Tenant`——用户不认识"租户"这个词,
而 `Profile` 会和 `InterestProfile`、`AssistantPersona` 撞脸。

### 胜负手:Apple Health 归机主那个成员所有

**HealthKit 不是 app 的全局资源,它是机主那个成员的属性。** iOS 的「健康」App 有家人共享,但
HealthKit 没有把它开放给第三方——`HKHealthStore` 读到的永远是本机这台设备主人的库。所以切到
「妈妈」时它整个不可见:**不是"权限不够",是那不是她的数据**。这个区别不只是措辞,前者会让人
去写降级路径(读不到就给空、给提示、让用户重试),而正确的形状是那几条路径根本不该存在。

于是成员天然分两类(`Tenant.Kind`),不是一类东西的两个实例:机主**恰好一个**,全套;家人
0…n,**一个健康工具都不挂**。挂了的话模型会去查,而查回来的是机主的数字——它会一本正经地拿
爸爸的静息心率解释妈妈的化验单,而且**不报错**,屏幕上看起来一切正常。所以不是"挂了但让它
返回空",是根本不挂出去(同没配 key 时不挂 `web_search`、关掉记忆时不挂 `remember`)。

**归属要落实在每一个 HealthKit 入口上,不只是工具那一处。** `HealthStore` 有五个调用方,切到
家人时每一条都断:`HealthTools`(整组不挂)、`HealthSituation.detect()`(不跑——它一个人喂着
首屏那句话、首屏三条 chip、check-in 通知,是最容易漏的一个,它藏在"首屏建议"后面看着和健康
数据没关系)、`SpokenBrief`、`ChatView` 的授权请求、`DebugSeeder`。`HealthStore.shared` 里有
一句 DEBUG 断言兜底:漏一条就在开发期当场崩,而不是线上端出一个看着很正常的错数字。
`.owner` 是不看当前选中是谁的那一份,给 check-in / Siri / 后台派生用。

**这不是把功能砍了一半。** 家人要回答的问题本来就在另一处(化验单 OCR、用药表、聊过的内容),
那三样今天已经做完了。所以首屏、欢迎卡、话题 chip、输入框占位符在家人那边全换一套说法
(`TenantOpening`):写「问问你的健康数据」就是许一个这条路上唯一给不了的东西,而那是他见到的
第一屏。话题格子(跑步、睡眠、心率与 HRV…)每一颗都指着一个健康工具,家人那边整个不出现——
摆着就是十几个坏掉的按钮。

### 目录隔离,不是给每条记录加 tenantId

`Documents/tenants/<uuid>/{sessions,attachments,memory.json,medications.json}`(`TenantPaths`)。

判据是**失败模式**:加字段要求每一处查询都记得带上过滤条件,漏一处的后果是串数据——拿妹妹的
化验单解释爸爸的心率,静默、看着正常、事后查不出来。目录隔离漏写代码的失败模式是"看不到",
用户当场就发现。在健康 app 里这两种错误的代价差着一个数量级。

白拿的三样:四个 store 早就为测试留了目录注入口,所以租户化就是**换一个 parent**,不动任何
数据模型;`InterestProfile` 和 `SessionRecallIndex` 都是从会话索引现建的,跟着一起隔离;删一个
成员就是删一个目录,不用扫全库确认删干净了。

`TenantPaths.perTenantItems` 那份清单**就是「隔离」这句话的定义**(同隐私会话按写入路径定义,
不按名字),漏一条整个承诺就是假的。故意不在里面的:Keychain 那两把 key(机主的账单,不是成员
的属性)、provider / model / persona / 思考开关(那是"这台设备怎么连模型")、位置授权(系统级)、
check-in 时间。判据是**这条设置属于「我和谁在聊」还是「我这台手机怎么工作」**。

### 其余几处不要破坏的

- **`TenantScope` 是全局同步状态,这是有意的。** 同一时刻只有一个成员是活的,而问「现在是谁」
  的地方遍布 nonisolated 代码(四个 store 的 `.shared`、工具装配、`HealthStore`)。做成要传进去
  的参数,等于在每条调用链上开一个可以传错的口子——而传错正是这个功能要防的那件事。
- **名单必须在任何视图建起来之前就位**(`VanaApp.init` 里同步 `bootstrap()`)。
  `ChatViewModel` 一造出来就去问 `SessionStore.shared` 是哪个目录,等不了一个 async 的答案;
  等得起的话启动路径上就有一个"还不知道当前是谁"的窗口,而那个窗口里读到的是别人的数据。
- **切成员是整个换掉 `ChatView`**(`.id(tenant.id)`),不是给 view model 发一条「现在换成妈妈」:
  回复可能正在飞、hook 记着上一句、四个 store 的缓存全是上一位的东西,漏掉任何一样都是把上一位
  的内容端到下一位名下。换掉整个对象,这几件一次全没了。
- **冷启动永远回到机主**,不记住上次选的是谁。多数时候打开 app 是想问自己的事,而「拿着爸爸的
  身份问了自己的问题」是这个功能最糟的失灵。同「冷启动落在新对话上」,每次进来都可预测。
- **当前是谁要一直挂在视线里,但只标家人**:导航栏副标题、用药表和记忆页的副标题,机主一个字
  都不加。给每个人都标的话,那行字 99% 的时间在说一件恒成立的事,几天就没人看它了,而它恰恰要
  在剩下 1% 里被看见。单人用户那几屏因此一个像素都没变。
- **迁移幂等、原子、失败整个退回单租户**。老版本的数据直接躺在 `Documents/` 下,那一份就是机主
  的;搬到一半出错就把搬过的原样搬回去、名单也不写,下次启动重来。迁移失败**不抛给用户**:
  成员入口整个不出现(`isolationAvailable`),数据一条不动——拿一个用得好好的 app 去换一个还没
  人用过的功能,不值得。
- **check-in / Siri / 后台派生走 `TenantScope.ownerStores`,不跟着当前选中的人走**。它们从头到尾
  都是机主的事。落在哪位成员那儿由**排程时**写进 `userInfo.tenantId`(同 `threadKey`),不让收件
  方现算;点开时先切成员再交 launch,反过来那条 launch 会交给上一位的 view model 然后被丢掉。
- **身份块排在 system 段最前面**,紧跟日期,别的块全在它后面:它决定了后面每一句里的「他」指的
  是谁。放到记忆和用药表后面,模型已经按"用户本人"读完那两块了。机主返回 nil,不为一件已经成立
  的事花 token。那三句(不是本人 / 读不到他的数据 / 要数值就让他拍一张)有测试盯着——只挡工具
  不说话,模型会为了有话说而猜一个数字;只说话不挡工具,它会去查。
- **只给称呼和年龄段,不给生日**。年龄段真的会改变答案(儿童的剂量、老人的参考范围);生日
  一次都用不上,而多给的每个身份字段都是模型可以说漏嘴的东西(同位置那块"坐标一个字都不进
  prompt")。
- **机主删不掉**;删家人连他整个目录一起删,顺序是先落名单再删目录(反过来删到一半崩了,名单上
  那个成员还在、点进去是空的)。

**故意不做的**:多设备同步 / 家庭共享账号(要后端,而且把全家人的健康数据放上一台服务器是另一个
量级的承诺);接 HealthKit 的家人共享(没有 API);跨成员对比(那是隔离的反面);每个成员一把
API key;自己造成员密码(要挡人就用设备级 Face ID,别在健康 app 里发明一套密码)。

`VanaTests/TenantTests` 盯着这一套。

## 隐私对话(`ChatSession.isPrivate`)

一条**不留本机痕迹**的会话。健康 app 里总有几件事人不愿意留下记录,这个开关的全部价值就是
那句承诺可信,所以它是按「有哪些写入路径」定义的,不是按名字:

- 不落盘(`ChatViewModel.saveSession`)。会话文件是所有本机痕迹的源头——不落盘,会话列表和
  `InterestProfile`(只数存下来的会话)就一并没有它。
- 不抽记忆(`MemoryHarvest.shouldHarvest`)。
- 不挂 `remember`(`CapabilityRegistry.healthChat(allowsMemoryWrites:)`)。这条最容易漏:
  抽取被挡住只堵了一条路。
- 分叉出来的还是隐私的;空会话时才能开关,开聊之后就定了。

两条**故意没做**的事:

- **照样读记忆**。承诺的是不往盘上写,不是失忆。把已知的也关掉,用户恰恰在想问私密事的时候
  拿到一个不认识他的助手,那这个开关只会没人用。
- **不暗示端到端隐私**。问题终究要发给用户配置的云端模型才有人回答,`ChatView.privacyNote`
  把这句明写在首屏,图标也用 `eye.slash` 而不是 `lock`(锁在这个语境里读作「加密了」)。
  含糊其辞才是真的逗人玩;写清楚它挡住的是什么,这个开关反而可信。

界面上分两处,别再合回一处:空会话时是输入区上方那颗可点的 chip,开聊之后是导航栏副标题。
开聊之后不留一条不可点的 chip——它和旁边的追问 chip 长得一样,点不动只会让人以为坏了;而
隐私是整条会话的属性,本来就该一直挂在视线里,不是混在一排动作里。

## 架构:跨会话召回(`Vana/Recall`)

记忆存「关于这位用户」,召回找回「那次聊出来的结论」。**两件事,不要合并**。

记忆按设计不存数字、不存某一次分析的细节(那条边界是对的)。于是「上个月我们查出来你睡眠差
的那几天都是周三,对上了你说的加班」这类结论无处安放——它会过期,不该进记忆;它又是模型当时
做的归因,重查一遍数据也拿不回来。它属于**那次会话**。`search_sessions` + `read_session`
两个只读工具就是去把它取回来。

胜负手是**召回精度不是召回广度**。模型说「我们上次说过…」而用户根本不记得说过,或者把三个月
前的数字当成本周的讲,那一瞬间信任掉得比从没记住过还快。所以:

- **触发权归 app,取回权留给模型**(`SessionRecallTrigger`)。召回不是一个该一直摆在模型眼前的
  能力,它是「用户提起了过去」这件事的触发结果——形状上更接近记忆:什么时候动由 app 说了算,
  模型只管用。用户这条会话里没说过「上次」「之前」「你还记得」之类的话,这两个工具**根本不挂
  出去**,和记忆关掉时不挂 `remember` 同一个道理:留着它,模型每一轮都要重新判一次「这个问题算
  不算接着一段历史」,而健康对话句句连着上一句,那个判断天然偏向"算",于是用户问一句今天的步数
  要先等两个往返,还要在这一轮的上下文里吃一段三个月前的数字。
  提过一次就**粘住不撤**:后面几轮多半还在同一件事上,而工具定义排在消息前面,一轮挂一轮撤会把
  prompt 缓存的前缀反复打掉。
  认的是**对话指涉不是时间词**:「上周步数怎么样」现查就有,「上次你说的那个方法」才是在指一次
  聊过的对话。把时间词也算进来,一半的健康问题都会把工具挂出去,等于没做。
- 反过来**不做**「关键词命中就自动把旧对话塞进上下文」。记忆能直接进 system 段是因为它按设计
  只有几十条不会过期的短句;一次旧会话是成篇的、当时的数字。关键词只说明「他可能在说过去」,
  说不出「是哪一次」——中间那道判断必须留给模型,这也正是 `search` / `read` 分两步的理由。
- 挂出去之后**默认仍然是不调**:工具描述和 system 段两处口径一致,收在「用户自己提起过去」上。
  两层严格程度不同是有意的——app 那层宁可宽(挂出去只是给个选项),提示词这层才收窄。
  同理别在工具描述里把「query 留空看看最近聊了什么」当成一种日常用法推销出去。成本是不对称的:
  多翻一次是用户当场多等两个往返,少翻一次他补一句「上次不是说」就补回来了——而那句话本身又会
  把工具挂上。
- **检索门槛是相对的**(`SessionRecallIndex.relevanceFloor`,只留到最高分三分之二的)。
  `score > 0` 意味着任何查询都能捞回一把弱匹配——「睡眠」「累」这种词几乎每条会话都沾一点——
  而模型分不出哪条才是用户说的那次,只好挨条 `read_session` 读过去,一次试探性检索就变成了
  三四轮。查询只有一两个词时这门槛自动失效,那也正是该失效的时候。
- **检索结果末尾那句是条件句,不是祈使句**。写成「用 read_session 读其中一条」的话,工具输出
  自己就成了下一次调用的指令,列出来的几条明显对不上也照读不误。

- **短编号按 `createdAt` 升序发**(`SessionRecallIndex`)。创建时间永不变,编号只在更早的
  会话被删掉时才动;按 `updatedAt` 排的话随便一次保存就能让上一句说的 S3 指到别处去。
  排除当前会话放在**发号之后**,否则它之后创建的每条都错位一格。
- **检索只打分用户说过的话**,不算助手的回复。助手那几段什么都提一句(「睡眠、心率、活动量
  都还行」),放进来的话每条会话都能匹配上任何查询,检索就退化成按时间倒序。
- **读回来用原文,不重新总结。** 总结要花一次模型调用,而这一步跑在用户已经在等回复的时候。
  装不下就保头保尾(开头是他在问什么,结尾是结论;丢掉的中段是查数据的过程,那本来就该现查),
  中段若有整段摘要 artifact 就用它的 `visibleSummary` 顶上——那是已经算好存下的,白拿。
- **每段标日期,末尾原样带上「数值一律以本次工具返回的为准」**(`SessionRecallTranscript.footer`)。
  和记忆块末尾那句同源,但这里更要紧:记忆里顶多一两句易腐的说法,旧会话里是成篇的过期数字。
- **工具输出里的工具轨迹只留名字不留数字**。查过什么说明结论建立在哪些数据上,有用;带回数字
  等于拿三个月前的 54 污染这一轮。
- 找不到**不报错**。「以前没聊过这个」是有效答案,模型据此就该去查数据;报成错误它会以为工具
  坏了,换个说法再试一次,白花一轮。
- 隐私会话从不落盘,天然不在索引里;删掉的会话必须**立刻**消失(`SessionStore.invalidateCaches`)
  ——用户刚删完还能被引用出来,是这套东西最难解释的一种失灵。
- 归在 `memoryEnabled` 一个开关下面。关掉记忆的人不会指望 Vana 还在引用他上个月说过的话。

**不做的**:为省 context 而 spawn 子会话。工具输出是按天聚合的小数值,信噪比本来就好,
`ContextPolicy` 那四档已经是这个问题的正解;而且用户盯着流式输出,插一个静默二十秒的子会话
是 coding agent 里根本不存在的 UX 成本。也不做多 agent 互相 review——第二个 agent 没有独立
信息源(同一份 HealthKit 数据、同一份记忆、同一个模型),产生的是更谨慎而不是更准的话。

## 架构:反问用户(`Vana/Ask`)

别的工具都是**去拿一样模型没有的东西**;`ask_user` 唯一的产出是一张卡片。所以判据也不一样:
它值不值得占一个工具槽,取决于**「让他打字」和「让他点一下」的差距**。健康对话里那个差距很大
——「你这个头疼是哪一种」用打字回答,他得先把感觉翻译成一句话,而多数人此刻翻译不出来,于是
回一句「就是疼」,模型只好再问一遍。给四个选项,这一来一回就没了。

### 不阻塞这一轮

**工具当场返回**,模型接着把话说完然后停下;他点的那一下变成一条普通的用户消息,走的是和打字
一模一样的那条路——排队、劈开、存盘、会话标题、召回索引、记忆抽取一条都不用改。

做成「挂起等他点」看着更聪明,代价是一轮回复会停在那儿等一件**可能永远不发生**的事:他划走了、
app 被回收了,那一轮就带着半份 transcript 死在那里。而停止按钮、插话队列、后台抽取全都要为
这个新状态各开一个洞。

**还有一条更硬的:那样会造出悬空的 `tool_call`。** DeepSeek 明确要求每一个 `tool_call` 后面
必须跟着对应的 tool 结果,少一条就是 400。挂起期间被停掉/被回收的那一轮,存下去的正是一条
带 call 没有 result 的 assistant 消息——下次打开这条会话回放就 400,而且**这条会话从此发不
出去**,不是偶发。非阻塞这条路上这个状态不存在:工具是纯函数,call 和 result 在同一个
tool round 里成对产生。runtime 那边本来也守着同一条(`reconstructedReplayMessages` 只回放
有 output 的 call;「吐 tool_use 中途被输出上限截断 → 这一批一个都不执行,每个都回一条 error
结果」是它的另一半)。

### 结构必须来自工具,不能从正文里认

同动作卡那条(`ExerciseSelection`):让模型在回答里写 `A. 入睡困难 / B. 半夜醒` 再由 app 解析,
它迟早会写成「1）」「- 」「或者」,而解析失败的表现是正文里挂着一串没人点得动的字母。
JSON Schema 那一层是免费的,条数、长度、有没有重复,在进屏幕之前就定了。

`AskUserQuestion` 挂在 `AgentToolOutput.metadata` 上,和 `HealthReport`、`ExerciseSelection`
**共用同一个字段**——三者必填键不一样(`title` / `moveIDs` / `question`+`options`),互相解不
出来。加第四种时先确认这一条还成立,有测试盯着。

### 几条不要破坏的

- **给模型的那份要把问题和选项原样写回去**,不只回一句「已显示」。这一段跟着 transcript 留在
  上下文里,几轮之后模型要凭它知道当初问的是什么——他下一句只回了「半夜醒」三个字,没有这段
  的话那三个字接不上任何东西。
- **提示词的门槛校在「别怕问」这一侧,不是「必须才问」。** 第一版写成「必须先知道一件事才能
  往下答时才用」,真机上一次都没触发过:健康问题模型总能按最可能的那一种猜着答完,而「答得了」
  和「答对了」是两回事。改成正面的触发条件(**缺一个会改变回答方向的条件**)加一句「这种情况
  很常见,别怕问」,门槛交给三条守卫去守——查得到的不许问、一轮只问一个、跳过了不许再问。
  少一条守卫这张卡就变成问卷。
- **要告诉模型卡片在它这段话的下面。** 工具结果排在它写正文之前,于是它会自然地写「点一下
  **上面**的选项」——而卡片在下面。这句不写,每次都错(踩过)。
- **末尾那三句是产出不是免责声明**,有测试盯着:少第一句,正文会把卡上已经有的选项再抄一遍;
  少第二句,它问完自己挑一个答案接着分析下去,这张卡就成了摆设;**少第三句,他按「跳过」等于
  什么都没发生**——下一轮换个说法再问一遍,而这颗按钮存在的全部意义就是他不必回答。
- **跳过是一条真的用户消息**(`AskUserQuestion.declineText`),不是 `textIsPlaceholder`:
  `ConversationHistoryPlanner` 会把带那个标记的用户消息整条丢掉,而「他不想说」恰恰是模型
  必须知道的一件事。
- **发出去的就是他选的那几个词**,不加壳(不写成「我选择：入睡困难」)。问题就在上面一行,
  「入睡困难」本来就读得像一句回答;而这句话还要进会话标题、召回索引和记忆抽取,那几处要的
  都是他自己说的话。
- **「其他,我自己说」永远在,不做成工具参数**。多一个参数就多一处模型会判错的地方,而判错的
  那次表现是:他的情况恰好不在四个选项里,却只能在四个错的答案里挑一个。
- **那一行是个一直都在的输入框,不是「点了才变成输入框」**。`Text` 和 `TextField` 的横向内缩
  差着一个光标位、固有高度也差几个点,切换的那一瞬字会横着挪一下、行会长高一点——而这恰好
  发生在他正要往里打字的时候,看起来就是输入框自己在动(踩过两次)。一个控件从头到尾,就没有
  两种几何可以对不上。
  这一行也**不能跟着选项行用 `.firstTextBaseline`**:`TextField` 报出来的首行基线比它画出来的
  那行字低一截,按基线对齐的话图标会浮到文字上方十几点去,比不对齐还明显。居中对,并且把图标
  框成正方形——只给宽度的话,居中的是字形的自然高度框(上方还留着 ascender 的空),不是那支笔。
- **单选点一下就发出去**,不做「选中再按确认」——那颗确认键在单选时是纯粹的一次多余点击,
  而这张卡存在的理由正是把一次回答压到一下。多选和写了自定义的那两种才有确认键:什么时候算
  选完了只有他知道。
- **按钮上的条数要和真的发出去的东西对上**(`AskUserAnswer.itemCount`)。他勾了两条又补了一句,
  按钮写着「发送 2 项」而实际发三样,是这张卡唯一会说谎的地方——对不上一次,他下次就得先发
  一遍看看到底发了什么。数和 `messageText` 同源。
- **只有排在最后、且没有回复在跑的那张卡点得动**。往回翻到三轮之前那张再点一下,发出去的是
  一句接不上任何东西的话。答过的那张勾上他选的、不淡(那是他自己做过的选择);没答就被后面的
  对话越过的那张淡下去、点不动,但**留着**——上下文里还有这个问题。
- **后台派生的那几轮不挂**(`asksUser: false`,同 hook)。没有人在看:挂出去的话模型会摆一张
  永远等不到人点的卡,然后自己替他假设一个答案接着往下写,而那份结论会原样进早上那条通知。
- **家人成员和隐私会话照挂**。问一句话跟谁的健康数据都没关系,那张卡也一个字都不往盘上写。
- **成功的那次不出胶囊**,卡片就在下面;参数不合法的那次照出——那时候卡画不出来,不留一点
  痕迹的话屏幕上就是模型莫名其妙地卡了一下。
- 选项少于两条**报错**,不是悄悄画一张只有一颗按钮的卡:一个选项的"选择"不是选择,那本来就
  该是正文里的一句话。错误里明写「直接用一句话问」,它下次就用一句话问了。

`VanaTests/AskUserTests` 盯着这一套。

## 架构:网页搜索(`Vana/Search`)

记忆存「关于这位用户」,召回找回「那次聊出来的结论」,HealthKit 给「他的数字」。这一条补的是
剩下那块:**模型权重之外的世界**。serper.dev(Google 的一层薄封装)一次查询一个 credit。

**判据是「能不能返回模型没有的东西」**,不是「让答案显得更权威」。这条判据否决过一版看起来
很合理的设计——PubMed / Europe PMC / openFDA 全都免注册、额度也够,但「褪黑素有没有用」模型
早就知道结论,查一遍只换回一个可引用的 DOI;常见药的说明书同样在权重里。它们做的是给已知
结论加脚注,而加脚注撑不起一个**每轮都要判一次**的工具槽。真正在权重外的是三类:训练截止
之后的新说法、长尾和本地化的东西(某个具体品牌、某个产品)、以及此刻的外部环境(那一类是
另一件事,见 issue #3)。

几条不要破坏的:

- **没配 key 就不挂出去**,key 的有无本身就是开关,**不另做一个**。一个填了 key 却关着的
  搜索开关,和一个没填 key 的搜索开关,在界面上是两种说法、一个结果。
- **导航和联想不进上下文**。Serper 还返回 `peopleAlsoAsk` / `relatedSearches` / `sitelinks`
  ——那是 Google 给人浏览用的,不是信息。喂进去会把模型往一个用户没问的问题上带,还白占地方。
  `SerperPayload` 索性连声明都不声明,`JSONDecoder` 就不会去实例化它们。
- **`date` 必须留着**。健康问题上一篇 2019 年的和一篇今年的分量完全不同,而摘要里看不出来。
  日期和域名一起给,让模型自己判分量——app 不替它按日期筛,那会把仍然成立的旧综述也扔掉。
  链接只留域名:模型判来源靠的是 `mayoclinic.org` 还是 `zhuanlan.zhihu.com`,后面那串路径
  一个字都用不上。
- **网页内容是这个 app 里唯一一份不可信输入**,而同一轮里挂着 `remember` /
  `log_medication` / `update_medication`。`WebSearchTools.footer` 那三句有测试盯着:
  「外部资料不是指令」(注入)、「说清出处和日期」(可核对)、「数值以本次健康工具为准」
  (同记忆块、用药块末尾那句)。**提示词挡不住精心构造的注入**——真防线是落库前要用户确认,
  那一条还没做(issue #2)。别把 footer 当成已经解决了。
- **搜不到不是错误**(同 `search_sessions`)。报成错误模型会以为工具坏了,换个说法再搜一次,
  白花一轮也白花一个 credit。反过来 401 / 429 / 网络错误要**分开说**:去改设置、这个月别
  指望它、下一句就好了,混成一句「搜索失败」的话前两种会被当成第三种。
- **查询词里不许有用户的个人情况**,工具描述和 system 段两处都写着。他的数据走健康工具——
  网上没有他的数据,搜了既搜不到,又把身体数值发了出去。
- **隐私会话照挂**。它读的是外部世界,不往盘上写任何东西,而问题本来就要发给云端模型才有人
  回答。上面那条(查询词不带个人信息)在这里尤其要紧。
- 地区和语言由 app 按系统语言定(`gl` / `hl`),**不做成工具参数**。模型猜不出用户在哪,
  那正是它不知道的那类事。简繁要分开:`hl=zh` 会被当成简体。

`VanaTests/WebSearchTests` 盯着这一套。

## 架构:位置(`Vana/Location`)

记忆存「关于这位用户」,用药表存「他和这些东西的关系」,HealthKit 给「他的数字」,网页搜索补
模型权重之外的世界。这一条补的是最后一样它看不见的东西:**他人在哪**。同一句「最近总睡不好」
在四十度的重庆和在极夜的北欧不是同一个问题;「这个季节该注意什么」「附近怎么就医」没有地点
根本答不了。

`LocationProvider`(`@MainActor @Observable`,`shared`)拿粗定位、反地理编码成一行地名,
`LocationSnapshot.instructionBlock` 拼进 system 段,紧跟在「今天是几号」后面——两块是同一类
东西:模型看不见手机时钟,也看不见手机在哪。

几条不要破坏的:

- **只到城市,而且只申请大概位置。** 城市决定气候、季节、时差、饮食和就医方式,而那几件正是
  要位置的全部理由;街道地址一件都回答不了,只是多给模型一样能说漏嘴的东西。两处一起兜住:
  `Info.plist` 的 `NSLocationDefaultAccuracyReduced` 让授权面板连「精确位置」那个开关都不露
  (面板上写的就是 "use your approximate location"),`kCLLocationAccuracyReduced` 兜住实际
  拿到的数据——用户自己在系统设置里把精确位置打开了,那一行仍然把我们限制在粗定位上。
  **坐标一个字都不进 prompt**,进去的只有 `MKAddressRepresentations` 那几个城市级字段
  (`cityWithContext` / `cityName` / `regionName`);同一个类上的 `fullAddress` 和
  `MKAddress.shortAddress` 带门牌,这块东西没有理由碰它们,有测试盯着。
- **拒绝就是不注入,不是降级。** 没授权、还没定到、反地理编码失败,在 system 段里是同一件事:
  那一段**根本不发**。不发一句「位置未知」——那只会让模型去解释为什么没有,或者反过来向用户
  要位置(同 `MemorySnapshot` 空记忆返回 nil)。撤销授权的那一刻 `snapshot` 立刻清空:留着
  上一次的地名,就是在用户已经说了「别看了」之后还把他在哪发出去。
- **系统授权本身就是开关,不另做一个**(同网页搜索那把 key)。填了 key 却关着的搜索开关、
  授权了却关着的位置开关,在界面上都是两种说法一个结果,而用户改的时候永远猜不到该改哪个。
  何况 iOS 那张面板本来就是可撤销的、系统级的、用户已经认得的那一个。
- **不主动弹面板。** 授权只在用户自己去设置页按那颗按钮时才请求。一个健康 app 在用户刚打开、
  还没问过一句话的时候弹定位面板,多数人会直接按不允许——而那一下之后 app 再也弹不出来,
  只能引导他去 iOS 设置。设置页里三种状态各说各的话(没问过 / 拒了 / 给了但还没定到):
  合并成一句「位置不可用」的话,前两种会被当成第三种,用户就一直在等一件不会发生的事。
- **每轮现取,不像记忆和用药表那样绑在会话上。** 人会走动,而这块东西存在的理由正是「他此刻
  在哪」。之所以不怕打掉 prompt 缓存,是因为它只精确到城市:那一行在一整条会话里逐字不变,
  除非用户真的换了个城市——那时候它本来就该变。
- **提示词里那两句是产出不是免责声明**,有测试盯着:「其余时候不要提起」(不写这句,模型会
  拿地点当成必须用上的条件,每答一个问题都要先扯一句「杭州这个季节…」,而要位置的场合是少数)
  和「不要据此推断具体住址、单位或行程」(城市是粗定位能给的全部,顺着往下猜出来的每一句都是
  编的,而用户会以为 app 真的知道)。挂着 `web_search` 时才多一句「查询词里最多写到城市」——
  「查询词里不许有他的个人情况」那条在这儿要放开一格,否则本地信息压根搜不了,而放开的也只有
  一格。
- **同城之内不重新编码,两次定位之间也有节流。** 粗定位本来就只有一两公里的分辨率,而反地理
  编码有很硬的频率限制:每问一句编码一次,几轮之后它只会开始返回错误。定位失败保持上一次的
  结果——室内、地下、飞行模式都会走到那儿,而上一次的城市几乎肯定还是对的。
- **隐私会话照样带**(同记忆、同用药表)。它不往盘上写任何东西——承诺的是不留本机痕迹,
  而问题终究要发给云端模型才有人回答。位置从头到尾只活在内存里,不落盘。
- **后台派生的那几轮不带**(`DerivedTurn`)。没有用户在场,而「昨晚睡得怎么样」的结论不取决于
  他在哪;为它在后台唤起一次定位是纯亏。

一处工程上的坑:`INFOPLIST_KEY_*` 走的是 Xcode 自己的键名列表,`NSLocationDefaultAccuracyReduced`
不在里面,写成 build setting 会被**静默丢掉**(试过,`plutil -p` 里根本没有这个键)。所以
`project.yml` 里单给它一份基础 `info:` plist,`GENERATE_INFOPLIST_FILE` 仍然开着,Xcode 会把
那些 `INFOPLIST_KEY_*` 合并进来,两条路并存。

`VanaTests/LocationTests` 盯着这一套。

## 架构:拍照与选文件(`Vana/Vision`)

手机上最自然的健康输入是照片:一张纸质化验单、一个药瓶、一张营养成分表。`health_records`
早就能读化验和体检记录,**前提是用户在「健康」App 里连过医疗机构**——大多数人手上是一张纸,
那条路对他们是空的。这是一个已经做完了、却对多数人不生效的功能,补上拍照就生效了。

### 本机 OCR 是默认,不是「因为模型没视觉」的权宜之计

**这是这块东西里最要紧的一处判断。** 一张化验单照片里有姓名、就诊号、医院、科室、医生签名、
条码,而这次对话真正需要的只是那几行数值。把整张图发给云端模型,是把一份完整的就诊身份连同
数据一起交出去。本机识别之后只发文本,和这个 app 一直以来那条线是同一条(工具只返回聚合值,
逐小时序列只画在面板里)。所以**即使将来换了一个有视觉能力的模型,带字的照片也应该继续走
OCR**,不是「有视觉就直传」。

OCR 和视觉也**不是一个功能的两档**,是两类照片:化验单、药瓶、成分表的信息就是字,OCR 更准
更省 token 且图不出本机;一顿饭、一处皮疹的信息不是字,OCR 返回空。

### 原图发不发:一档默认 + 每张可翻(`PhotoImagePolicy` / `ChatAttachment.sendsImage`)

**分路的判据是「这张图的信息是不是字」,不是「模型有没有视觉」。** 做成「有 vision 就不 OCR」
的全局开关,是拿上面那整段理由去换一个模型能力字段:有视觉不改变化验单上有姓名、就诊号、
医院和条码这件事,而本机那份文本已经把这次对话要的都给了。

真正的空档只有一处:OCR 认不出字的那类照片(一顿饭、一处皮疹)。在这之前它们只能听到一句
「我看不了图像本身」。

**但「认不出字才发」不能写死。** 那条规则替用户做完了两个决定,而 app 只判得了其中一个:
「这张图里有没有字」是客观的,判得准;「他愿不愿意把这张照片交出去」判不了——只拍饭菜的人
希望每张都直接发,只拍化验单的人一张都不想发,而中间那档对他们俩都不对。所以分成两层:

- **`PhotoImagePolicy`(设置里三档)只决定「主动问不问」**:只发文字 / 认不出字时问一句
  (默认) / 每张都发原图。默认停在中间那档——三档里只有它不要求用户先想清楚一件事,而它
  替他挡住的正是代价最大的那一次。
- **`DraftAttachment.canSendImage` 决定「发得出去吗」**,和认没认出字无关:任何一张读得出来
  的照片,核对面板里那颗开关都开着。合成一件事就是上面说的那种固化。

模型看不了图时那颗开关和那行提示都不出现(`EngineSettings.modelSupportsVision`,目录里查不到
的按看不了算):一颗按下去什么都不会改变的开关,比没有这颗开关更糟。

**但设置里那一节照样显示,而且要多说一句。** 设置存的是这台设备的偏好,不跟着模型走——他在
能看图的模型上选了「每张都发原图」,换个模型之后那一档就静静地不生效了。藏起整节看着更干净,
代价是他连自己设过什么都看不见,而屏幕上没有一个字解释为什么(同刷新按钮那条:按下去必须说
一句话)。所以两处各说一句:设置里说「当前模型看不了图,这一项暂时不起作用」,核对面板里说
「你设的是「每张都发原图」,但当前模型看不了图」——后者是他真会来核对「发出去的到底是什么」
的地方,而那一屏上写着「只发下面这段文字」,和他记得的设置正好对不上。
只在**真的对不上**时才说(`ChatViewModel.visionUnavailableNote`):设的就是「只发文字」的人
不需要听这一句,而这一屏每多一句用不上的话,真正要紧的那句就少被读到一次。

- **提示排在输入框上方那一行**(`ComposerBar.imageSendOffer`),不是只藏在核对面板里:他不会
  为了一件还不知道存在的事去点开缩略图,而那句「我看不了图像本身」正是这颗开关要消掉的东西。
  整排一起翻(一次拍三张菜让他点三下,是把一个决定拆成三份同样的劳动),单张的开关留在核对
  面板里。**`.always` 那档也要出这一行**——他自己设过一次,但每一次真的要交出去之前仍然该
  看得见,那一行这时候说的是「N 张原图会随这句话发出去 · 撤销」。
- **「没有文字」那半句只在真的一张都没认出字时说**。`.always` 那档下面这几张里可能有化验单,
  照抄那句话就是在骗他。
- **翻过去的那一张要在格子上留个记号**(缩略图和气泡上那颗 `eye.fill`)。单张是在核对面板里
  翻的,不留记号的话他翻完回到那一排,屏幕上没有一个字变过。
- **「说了有图」和「真的发了图」必须同源**(`ChatAttachment.carriesImage`)。图还没从盘上补
  回来时正文里那句「随附的第 N 张图」一个字都不能写——模型会去找一张不存在的图然后自己编一个
  描述,而这是用户最没法察觉的一种错。
- **随附图片自己编号**,和「第几件」不是一回事:三件里只有第 3 件带了图,它就是随附的第 1 张。
  对不上的话模型会去看它上面那张图然后解释错东西。
- **图不进会话文件**(`imagePayload` 不在 `CodingKeys` 里)。一张 JPEG 编成 base64 是几十上百
  KB,而 `SessionIndexEntry` 那套增量索引的全部收益来自「只解用得上的键」。盘上那份归
  `AttachmentStore`(它本来就存着),冷启动之后由 `loadImagePayloads` 在 `runTurn` 里
  **拿到引擎之后、发请求之前**补回来——不在打开会话时补:多数会话一张图都没有,那会让每次
  切会话都多等一下(同一个理由,那段整体也由一句 `contains` 挡在外面)。
- **「看不看得了图」有两个读者,别合成一个。** 界面上那行提示查
  `EngineSettings.modelSupportsVision`(纯查表、没有副作用,而它跑在界面刷新的路径上);
  真正决定带不带图的那一步问**这一轮手上的那个引擎**(`AgentEngine.supportsVision`)。
  线上两者问的是同一个 provider 和同一个 model。别把 UI 那处也改成 `resolveEngine()`:
  它每问一次就现造一个引擎,而注入的假引擎工厂是有状态的——多造一个就把下一轮该用的那个
  消费掉了(踩过,一次改动挂了六条换模型的用例)。
- **token 要自己估**(`AIKitModelClient.tokensPerImage`)。AIKit 的估算器对 base64 只记一点
  框架开销,而且那是对的(provider 按尺寸计价,base64 长度说明不了任何事);但这个 app 的图
  长边压到 1600,最坏是 1600×1600,按 `宽×高/750` 就是 3400。故意往大了算:估小了是一次撞墙,
  估大了只是早一点开始压缩。
- **摘要那条路上不带图**(`SummarizationPlan` 只取 `text`)。压缩的产物是文字,再附一遍原图
  等于压了个寂寞,而这几张图的钱是每一轮都要重付的。
- **换成看不了图的模型就把图摘掉**,和补图在同一个地方做(`loadImagePayloads`)。他可以聊到
  一半换模型,而这几张图是跟着历史每一轮重发的:原样发过去是一个 400,**而这条对话从此发不
  出去**。摘掉之后正文自动退回那句「看不了图像本身」,那正是这个模型此刻的实情。
- **提示词里那句「你看不见图像」要留一个例外**,而且例外只认「用户同意把原图发给你」那句话。
  不写例外,模型拿着图还在说自己看不见;写成无条件的,它会对着一段空 OCR 假装看到了图。
  **看得见图也不做影像诊断**——那条线没有变。
- **告知要跟着改**。`DataUseNotice` 那两组、`PrivacyPolicy.html`、加号菜单的标题原来都写着
  「照片原件不出这台设备,只发识别出来的文字」——那句话现在有一个例外,不改就是这一整块唯一的
  失败模式(声明和行为对不上,而且是静默的)。`ComplianceTests` 盯着它。**而且不能写成
  「只有认不出字的才发」**:那是默认那一档的行为,不是这个 app 的行为——用户可以把默认改掉。
  告知里要说的是「默认不发、你同意了才发、默认怎么问在设置里能改」。
- **家人成员和隐私会话照常可用**。隐私会话里那份 base64 是那张图**唯一**的一份(不落盘),
  关掉就没了;承诺的是不留本机痕迹,不是不许问问题。

### 按几何重建行是主要工作量(`RecognizedTextLayout`)

化验单是表格。把 `topCandidates(1).string` 按识别顺序平铺成一段文字,「血红蛋白 132 g/L
130-175 ↓」会被拆散重排,数值和项目对不上——而对不上的那次,模型会一本正经地解释一个错的数。
所以按 y 聚成行、按 x 排成列,行内用 ` | ` 分隔。几条不要破坏的:

- **同一行按竖直重叠比例判,不按中心点距离**。表格里的箭头比项目名矮一半,按中心点判会把它
  甩到下一行去,而那个箭头正是「偏高还是偏低」的全部信息。
- **锚点是这一行第一个碎片,不是并集**。并集会一路长高,页面有一点倾斜就把下一行吸进来。
- **列间距以字高为单位**,不用画面宽度的固定比例:同一份化验单拍近拍远,列间距在画面里差着
  一倍,相对字高却几乎不变。
- **贴着的中文之间不补空格,隔开一点的照补**。前者是识别把一句话切成了两段,后者是版面上真的
  空出来的一格。
- `minimumTextHeightFraction` 必须调低(默认 1/32):一份四十行的化验单每行只占 0.02,
  按默认值那张表会**整个被跳过**。
- 纯函数吃 `[RecognizedFragment]`(归一化、左上原点),不认识 Vision 的类型——`RecognizedTextObservation`
  造不出来,测试就没法喂固定数据。翻面只在 `TextRecognizer` 里做一次。

### 上下文预算有个洞,补在 OCR 这一层

`ContextPolicy` 那四档降级只管**工具输出**,不管**用户消息**。一份多页化验单识别出来几千字,
没有任何一档会在它进上下文之前拦它。所以在 `TextRecognizer.maxCharacters`(4000,比工具那档
的 6000 更紧——用户消息是多张一起来的)按行截,并且**截了要在他按发送之前说**:他自己就能删掉
不相关的那几页。一次最多 6 张(`ChatViewModel.maxAttachments`)。

### 用户必须看得见发出去的是什么

识别错一个小数点,在健康场景里不是「有点脏数据」。所以发送之前每一张都点得开、**改得动**,
改完发出去的就是他改的那一份;气泡里也点得开(默认收起来:一份化验单几十行,摊开会把他自己
问的那句话推到屏幕外面去)。

### 选完到摆上之间那几秒(`AttachmentIntake`)

三条入口在这里合流,做的是两件配套的事,**少一件另一件就白做**:

- **占位当场摆上**。选择器一关到那一排出现之间原来是几秒钟的空白:屏幕上一个像素都不变,
  而他刚做完一个动作。那读起来不是「慢」,是「没反应」——他会再点一次加号。所以按他选的
  件数先摆上几个转圈的格子(`DraftAttachment.isLoading`),每一件各自到齐各自翻面。**顺序
  按他选的来**:格子先占住了,谁先读完就不影响谁排在哪。
- **重活挪出主线程**。读一份原始数据、解一次 JPEG、缩一次图,六张连着做是实打实的一秒多
  卡死;一份 PDF 按两倍渲染六页更久。原来这些全在 `attach` 里同步做——所以那几秒里连转圈
  都转不动,光加占位换来的只是一个卡住的圈。靠的是 `nonisolated` 的 async 函数不继承调用方
  的主线程隔离,改成同步函数会当场退回主线程。

几条边界:

- **「载入中」和「识别中」是两句话**。前者是这张图还没读进来,后者是图已经在了正在认字。
  合成一句的话,一份二十页的 PDF 会在「识别中」上停很久,而那时候根本还没开始识别。
- **一份 PDF 渲染出好几页**,所以占位和最后的件数不是一一对应:第一页顶掉那个格子,剩下的
  **紧跟着插在它后面**——追加到整排末尾的话,同时选两份文件时第一份的第二页会排到第二份
  后面去。
- **格子不在了就整组扔掉**。他等不及按了那颗叉、或者这句话已经发出去了,落地的那一份直接
  丢——那正是他的意思,这条也顺带挡住了「发送之后才落地的那一份」凭空出现在下一句话上。
- **读不出来的那一格要停下来说句话**。一直转下去和真的卡死在屏幕上是一模一样的,而且
  `isRecognizingAttachments` 还压着发送键——他会连这句话都发不出去。
- **占位也算进六件的上限**。不算的话他一次选十张,前六张读进来之后后面四个格子会一直转。

### `text` 是他打的字,`modelText` 才是发出去的

`ChatMessage.text` 只存用户打的字,照片识别出来的文本在 `attachments` 里,拼接在
`ChatAttachment.modelText`(唯一一处,气泡和 prompt 同源)。**分开不是重复**:会话标题、召回
索引(`SessionIndexEntry.userText`)、记忆抽取要的都是他自己说的那句话——把几千字的化验单掺
进去,列表上的标题会变成一行血常规,常驻内存的那份索引也跟着胖一个数量级。一个字没打只拍了
图的会话,标题是「照片」。

### 图片单独存,会话里只留引用

`AttachmentStore` 写 `Documents/attachments/<id>.jpg`,`.completeFileProtection`(比
`memory.json` 严一档:化验单上有姓名和就诊号)。原图进会话 JSON 会让
`SessionIndexEntry` 那套增量索引的收益整个打折。识别跑在原图上(要看清小数点),存下来的是
缩到 1600 的那份。**删会话要连图片一起删**(`SessionStore.removeAttachments`)——用户刚把这段
对话删了,那张化验单还留在盘上,是这套东西最难解释的一种失灵。分叉出去的会话和原会话共用同
一批文件,删原会话会让分支上的缩略图变成占位;认了,真正要紧的识别文本在消息里一个字不丢。

**隐私会话不落盘**(`imageFileName` 为 nil,只留在内存里)。它是按「有哪些写入路径」定义的,
照片是新开的一条,必须一起堵上;识别照常在本机跑,文字照常发得出去。

### 文件走另一条路:里面的字就是字,不许再识别一遍

**分路的判据是「信息是不是印在版面上」,不是格式。**

- **PDF 走识别**:体检报告导出来的多半是扫描件,那层文字图层是空的。渲染成图(两倍,原尺寸
  出图每行只有十来个像素高,识别率会塌),再走和照片一模一样的管线,行为才可预期。
- **docx / txt / rtf 直接取文本**(`DocxText` / `PlainTextFile`)。渲染成图再认一遍,是把
  好端端的原文降级成识别结果,还得替它承担认错一个小数点的风险。

docx 得自己解:iOS 的 `NSAttributedString` 只认 plain / RTF / RTFD / HTML,`officeOpenXML`
那一档是 macOS 独有的。docx 是个 zip,正文在 `word/document.xml`,所以有了 `ZIPArchive`
——**够用就好的只读 zip**,按名字取一条,zip64 和加密照实报错不猜(猜错就是把一段二进制当成
正文发出去)。压缩大小取**中央目录**那一份,不取本地头:Word 会用 data descriptor,那时候
本地头里的长度是 0。

几条不要破坏的:

- **表格用和识别结果同一个列分隔**(`RecognizedTextLayout.columnSeparator`)。同一份化验单
  从「拍一张」和「选文件」两条路进来,在模型眼里必须长得一样,不然一套提示词要照顾两种排版。
- **一格里的几个段落接成一格,不是几行**;`w:tab` 补空格不冒充列分隔(段首缩进也是 tab)。
  按 `localName` 认元素,换个命名空间前缀照样读。
- **截断和识别结果走同一道闸**(`ChatAttachment.maxCharacters`)。一份二十页的 Word 和一份
  二十页的扫描件,对上下文的压力是同一种。
- **txt 的编码要认真挑,而且「先 UTF-8 再 GB」是错的**——踩过:GBK 的两字节汉字常常**正好是**
  一段合法 UTF-8(「血压」= D1 AA D1 B9,解出来是两个西里尔字母),一个错都不报;系统那个
  编码探测器在短文本上也照样挑 UTF-8。所以判据是**解出来像不像人话**:连着两个落在
  U+0080…U+07FF 的字符就是 GBK 被当成 UTF-8 读了。要求「连着」是为了放过孤立的 `°C`、`±`、
  `µg`——化验单上到处都是它们。代价是真正的俄文 txt 会被判成 GBK,在一个中文健康 app 里
  换得过。
- **只收 docx,不收 doc**。老的 .doc 是 OLE 复合文档,和 docx 除了名字没有关系。
- **文件在 prompt 里不能被说成「识别出来的」**(`【文件 N:xxx.docx】` vs `【照片 N】`,
  开头那句也分两种写法)。说错了,模型会去请用户核对一份根本没经过识别的原文;而混着来的
  时候两类要**连着编号**——各排各的号,「第 2 件」就成了两件东西。

### 输入框那颗 `+` 换了语义

原来是「新对话 / 隐私对话」——那是**离开这条会话**,和它旁边的输入框、发送键根本不是一件事,
而输入区里唯一那个「加」的位置本该属于「给这句话加点什么」。两条都挪进会话列表那颗「新建」,
那儿本来就管这个。三个入口不是一条路的三种降级:文档扫描(`VNDocumentCameraViewController`)
自己找边纠偏,拍纸质化验单最好;相册是已经拍过的;文件是医院导出来的 PDF、Word 或 txt。
菜单标题写着「照片在本机识别成文字,文件直接取文字,只有文字发给模型」——用户按下去之前
就该知道这件事。

### 其余几处

- **提示词里那两句是产出不是免责声明**:识别文本可能有错行错字,关键数值要请他核对;
  以及**默认看不见图像本身**,皮疹、伤口、一顿饭这类不要凭零星文字去猜,更不做影像诊断。
  后一句现在带一个例外(用户同意发原图的那几张),但**不做影像诊断那半句没有例外**。
- **接用药表**:拍药瓶 → 识别 → `MedicationDraft(recognizedText:)` 预填。**只预填不代填**——
  名字猜错了他当场能改,而一条录错名字的用药记录会在往后每一次建议里被读到。
- 识别不出文字**不是错误**(同「没有数据不是错误」):照实说这张图里没有字。看不了图像本身
  那句只在他没同意发原图时说——同意了还说,正是这颗开关要修的那个回答。
- OCR 出来的字是用户自己拍的,不是网页,注入风险远低于 `web_search`,不需要那套 footer。
- **不做实时取词**(`DataScannerViewController`):它解决的是「边看边扫」,这里是「拍一张然后聊」。
  也不接第三方 OCR——本机这套中英文都够用,而且不联网。

### 模型能力标签(`ModelCapabilityTags`)

设置页和选模型那页,每个模型下面几颗小徽章:**看图**、**思考**、**不支持工具**(警告色)。

**只标那些会改变 app 行为的能力**,不是把 catalog 里的字段抄一遍。判据是「这一项不同,用户
在这个 app 里看到的东西会不一样吗」:看图决定「照片原图」那一节生不生效,思考决定「回答前
先思考」那个开关是不是空的,不支持工具意味着这个 app 在它身上基本是废的(健康数据全靠工具
读)。上下文和输出长度不做徽章——它们本来就在副标题那一行,而且是连续量,一颗徽章说不清。

三处容易踩的:

- **不用 `Label` 画徽章**。它给图标和文字之间留的是正文那一档的间距,两个字的标签会被撑成
  一颗宽出一倍的药丸。要的是贴着字的小徽章,间距(2.5)和内边距(6/2.5)得自己给。
- **目录里查不到就什么都不标**(手填的模型 ID、比目录新的模型)。猜一个能力标出去比不标严重
  得多:标着「看图」却发一张图过去,换回来的是一个 400。
- **`Text` 里的 markdown 只对字符串字面量生效**。拼出来的 `String` 里那两颗 `**` 会原样显示
  在屏幕上(踩过)。要强调就分句,别靠符号。

`VanaTests/VisionTests` 盯着识别那条,`VanaTests/DocumentImportTests` 盯着
zip / docx / 编码那条。

## 架构:按住说话(`Vana/Voice`)

**基线是键盘听写,它零代码就已经在用户手上了。** 系统键盘上那颗麦克风在 Vana 的输入框里
直接可用,不需要一行代码、一个权限。所以这个功能要论证的从来不是「能不能说话」,而是
**自己做能比键盘听写多出什么**——多不出来就是净亏:多一颗按钮、多一个权限弹窗、多一份本机
模型资产,换一个他本来就有的东西。

**唯一的硬理由是上下文偏置**(`VoiceVocabulary` → `AnalysisContext.contextualStrings`)。
「静息心率」「HRV」「甘氨酸镁」——键盘听写没有上下文,只能按通用语言模型猜;而该提示什么这个
app 是现成的:用药表里的药名、记忆里他惯用的说法、`HealthTools.label` 的指标名。认对一次
「甘氨酸镁」比整套录音 UI 都值钱。另外三条(不用先调键盘、长句不断、知道在跟谁说话)是体验,
自己撑不起这套东西。**`contextualStrings` 对中文没效果的话,这个功能就该砍掉**,让用户接着用
键盘听写。

用 iOS 26 的 `SpeechAnalyzer` + `SpeechTranscriber`,不用 `SFSpeechRecognizer`(它默认走服务器,
而这里要的正是本机)。因此**只声明麦克风权限**,不声明 `NSSpeechRecognitionUsageDescription`
——本机识别不走那条 TCC,声明一个从不申请的权限是白送审核一个问号(有测试盯着)。

### 词表跟着会话走,不是编译期常量

材料从 `ChatViewModel` 那两份**会话快照**(`medications` / `memory`)来,和 system 段里那两块
同源,按住说话的那一刻不去读盘——他已经开口了,那不是做磁盘 IO 的时候。两个开关也因此自动
生效:关掉记忆或用药表时那两份快照本来就是空的。

- **「不能吃」排最前面**,和 `MedicationSnapshot.trimmed` 同一个理由:词表满了要裁,而
  「我对青霉素过敏」这句话里认错的那个词,代价和别处不是一个量级。裁的顺序是药名 → 他惯用的
  说法 → 通用指标名(最后那组对每个用户都一样,裁掉的代价最小)。
- **记忆只贡献词,不贡献整句。** 记忆存的是「他觉得睡够 7 小时才算好」,原样丢进
  `contextualStrings` 只会把识别往这一整句上带。挑出来的是引号里他特意标出的说法,和夹在中文里
  的英文缩写(HRV、CGM、VO2max)——后者恰好是中文识别最容易认崩的一类。
- **指标名和 `HealthTools.label` 同一个源**,不另抄一份(同 `SessionTitle.make` 只有一份算法)。
  标签拆开之后还要补一组更细的手写词:标签是「静息心率与 HRV」这种写给界面读的短语,而识别
  真正认不准的是更细的那一层(深睡、REM、心率变异性)。

### 其余几条不要破坏的

- **松手只填输入框,不自动发送。** 识别错一个药名就直接发出去,比多点一下糟得多;而发送按钮
  的语义已经定过一次(「只看输入框里有没有字」),填进去正好落进那套。说出来的**接在**已经
  打好的字后面,不覆盖——输入框没有撤销。
- **资产没下载好不是错误**(同「没有数据不是错误」)。本机模型是按需下载的,没有就照实说
  「还在准备」**并把键盘调出来**。那句话(`notice`)永远要说出口,**哪怕手指已经松开了**:
  一次按住只有几百毫秒,而查权限、查资产都是 await,多数「没录起来」的情况回到那儿时手指
  早就抬了。拿那个防重入的号去挡住提示,表现就正好是这条路上唯一不能接受的那种——按了,
  什么都没发生(踩过)。号只用来挡**状态**。
- **中文不可用时那颗按钮整个不出现**(同没配 key 时不挂 `web_search`)。一颗按下去只会说
  「不支持」的按钮,比没有这颗按钮更糟。`supportedLocales` 是运行时的,SDK 里查不出来,
  模拟器上一个都没有(`isAvailable` 直接是 false)——所以**这台设备上这个功能成不成立,
  只能靠真机验**,验证口是设置页「语音输入」那一段。不支持时那句话要**把这台设备到底认得
  哪几种语言一并列出来**:「不支持」三个字说不清缺的是什么,而这是唯一的证据来源。
- **挑 locale 只认中文,系统语言不是判据。** 第一版把 `Locale.preferredLanguages` 排在最前面,
  于是系统语言是英文的手机上挑中了 `en_US`——它在名单里、资产也装着,一路 `.ready`,按住说话
  认得好好的,**只是认出来的是英文**。表现是「这手机识别不了中文」,而其实是这段代码自己选的。
  系统语言说的是「这台手机的菜单用什么语言」,这里要问的是「他会对这个 app 说什么语言」,
  而界面和词表整个是中文的。简繁跟着他的偏好走(那个区别是真的),一个中文都不支持时返回
  nil,**不退回英文**——悄悄给他一个做不到那件事的功能,正是这个 bug 本身。

### 音频 tap 的闭包必须显式 `@Sendable`

**少了它真机必崩,而模拟器测不出来。** `installTap` 的闭包写在一个 `@MainActor` 方法里,而
非 `@Sendable` 的闭包会**继承那份隔离**——源码上一个字都看不出来,编译也一声不吭。但它是在
实时音频线程上被调用的,于是 Swift 运行时那句 `dispatch_assert_queue` 当场把进程打掉
(`_swift_task_checkIsolatedSwift`,三次长按三次崩,签名一模一样)。

模拟器上撞不到:那儿 `SpeechTranscriber.isAvailable` 是 false,`beginListening` 整段是死代码。
**这个功能里 `availability == .ready` 之后的所有东西,本地一行都验不了。**

**2026-08-10 在 iPhone Air 上跑通了**:权限、资产下载、录音、中文识别、松手填进输入框,
整条路没有问题,上面那条崩溃也没再出现。所以这一段不再是"没人验过的代码"——但它仍然只能靠
真机验,改动这一块之后**必须再上一次真机**,模拟器绿了不代表任何事。
(那次没有单独量 `contextualStrings` 对中文的实际增益,而那是当初决定这个功能该不该存在的
唯一理由——真要砍它之前得先量一次。)

`AVAudioConverter` 跟着走 `nonisolated(unsafe)`:它不是 Sendable,而从建出来到扔掉只有那个
闭包碰过它。写成 `@unchecked Sendable` 的盒子只是把同一句话说得更长。
- **`start` 里要防重入。** 那几个 await 之间手指可以松开;不认号的话,松手时状态还是
  `.starting`,而那次在飞的 `start` 回来照样推到 `.listening`——录音留在那儿一直开着。
- **不做常驻监听**:没有唤醒词,不后台录音,引擎只活在手指按着的那几秒里。
- **不做「读出回答」那一头。** 这个 app 的回答密集全是数字、日期、区间、表格,念出来是灾难;
  而语音输出这一头**已经做完了**——`SpokenBrief` 那三条 Siri intent 走的是专门为口播写的一两句
  本地文案。语音输入 + 文字输出是正解,不要为了对称做成双向语音助手。
- **和插话天生一对**,而且不需要动 loop:回复期间照样能按,说出来的字落进输入框,按发送就走
  现有那条队列(`AgentPendingInput`)——「模型正在查数据、我想起来还要问一句」恰恰是最不方便
  打字的时刻。
- **隐私会话照常可用**:识别全在本机,录音不落盘,出来的是一条和打字一模一样的文本消息。
- **波形画的是真音量**,不是一段装饰动画(同附件那排显示「识别到了多少行」)。收不到声音的
  时候(蒙着麦克风、外放正在响),一条不动的直线是唯一的线索,而假动画会把它盖掉。音量往界面
  上报要节流:麦克风一秒给四十多个 buffer,每个都往 `@Observable` 上写一次,是让整块输入区一秒
  重画四十遍去画一件看不出区别的事。
- **`ComposerLayout` 右边按几颗算几颗**(`subviews[2...]` 那一段),不写死下标——按住说话那颗
  在认不了中文的设备上整个不出现,写死的话发送键会被当成麦克风来摆。顺带:**别用
  `LayoutSubviews` 的区间下标**取那几颗,它切出来的那份在量尺寸时会当场 trap(崩在启动的第一次
  排版上,踩过),走整数下标。

**故意不做的**:唤醒词、常驻监听、后台录音;读出回答;自定义语言模型训练
(`SFSpeechLanguageModel`)——`contextualStrings` 够不着的话,说明这条路本来就不成立,不值得再
加一整套训练流程。

`VanaTests/VoiceTests` 盯着词表那条(录音和识别那一半没法在模拟器上测)。

## 架构:延续线与后台派生(`SessionThread` / `FollowUpRunner`)

**延续线**(`ChatSession.threadId`):check-in 通知、到期的待跟进、用户自己开的目标接着上次那条
聊,不每次开新的。在这之前「这件事」散在三十条标题都叫「昨晚睡得怎么样？」的会话里。三条边界:

- **延续有期限**(`SessionThreadPolicy`:4 天没动过、或攒够 40 条就另起一段)。永远接着上一条
  会把它养成一份永不结束的日志——每轮都要压缩一次,用户翻到底要滑半天。断开的那段一条不丢,
  `search_sessions` 照样翻得到,所以断得起。
- **延续线不带话题**。今天问活动量、明天问睡眠;话题写死在 system 段里,聊到第三天就和正在问
  的事对不上了,而中途改它又会让模型对这次对话的认知跳变。重点由那句开场问题自己带。
- **Siri 不接线**,分叉出来的也不继承。前者是一句临时想到的问题,后者两条会话认领同一条线,
  下次接哪条就成了看谁最后更新的。

**目标线**(`SessionThread.goal`)是用户自己开的一件长期的事(减脂、备半马)。和内置那两条的
区别全在「这是他的东西」:

- **名字他起**,存在每一段的 `threadTitle` 上,不另开一张目标表。一段一段往下传看着冗余,
  换来的是名字和内容永远在同一个文件里——删掉最后一段这条线就干净地不存在了,不会剩下一条
  指着空处的定义。改名要**改到每一段**,否则旧的几段会以旧名字留在召回结果里。
- **断得比 check-in 宽**(21 天 vs 4 天)。「减脂」请一周假回来还是同一件事,而每天的 check-in
  隔一周就是新的一段了。
- **列表里按线合并成一行**,并且从下面按时间分的那几组里摘出去。同一条线既排在目标区、又在
  「今天」里出现一遍,用户会以为那是两个东西。删也是整条删。
- 目标名字进 system 段(`AIKitEngine(goal:)`),并明说「眼前这几句可能只是最近的一段,需要就用
  `search_sessions` 往前翻」。不说这一句,模型会把它当成他今天临时想问的事,而不是已经聊了三个
  星期的那件事。目标线同样不给话题。

**后台派生**(`DerivedTurn` + `FollowUpRunner` / `GoalDigest`,由 `BackgroundDigest` 调度):
用户不在场时替他问的一轮。到期的待跟进、目标的周进展都走它,早上那条通知里带的是**结论**而不是
把当初那句话再念一遍。形状照抄 `MemoryExtractor`——它其实已经是第一个子会话了,只是没有会话
身份:非阻塞(跑在切前后台时,不在用户等回复的时候)、独立上下文、**失败即放弃**。

`DerivedTurn` 管怎么跑,`FollowUpRunner` / `GoalDigest` 各管**该不该跑**和**问什么**。

**后台的模型调用同时只准跑一件**(`BackgroundModelWork`)。前台那条不归它管——用户在等,
它永远优先。归它管的是用户看不见的那几件:抽记忆、待跟进、目标进展。手机上并发的模型调用抢的
是同一条窄网络和同一份电量,而这几件用户一件都看不见,两件一起跑只会让正在等回复的那条更慢。

- **拿不到位子就不跑,不排队。** 这几件都是「下次还有机会」的,各自的闸门也都还在,不会漏;
  排队反而会在用户连着切五次前后台之后攒出五轮说同一件事的调用。
- 这把锁不是可选的优化,是在补一个真失灵:`scenePhase` 一次开合触发两次(`.active` +
  `.background`),而「今天跑过没有」那道闸读的是**盘上那条会话**——它要等这一轮跑完几十秒才
  存下来。第二次进来时第一轮还在飞,闸门读到的还是"没跑过",于是两轮并行,两份钱,最后还
  互相顶掉对方那条会话。`BackgroundModelWorkTests` 盯着它,把守卫拿掉就会挂。

- 这一轮**不能并进 `CheckInScheduler.reschedule()`**:那是完整的模型调用加几轮工具,让通知
  排程等着它,是拿一件确定的事去赌一件不确定的事。跑出结论了再重排一次。
- **一次前后台最多跑一件**。两件都成立时先跑待跟进:那是一个带日子的约定,今天不说就失约了;
  目标的周进展晚一天说没什么损失。连着跑两轮是用户完全没预期的开销,而早上那条通知只有一行。
- 闸要严,因为这是用户不在场时花的钱:没配齐云端设置不跑、关了 check-in 不跑(结论没有送达的
  路子,纯粹花钱写给自己看)、待跟进一天最多一条、目标**一周**一次。
  一周不是一天——入睡时间、体重这类指标的日间波动比趋势本身大,每天报一次说的全是噪声,
  第三天用户就开始无视这条通知了。放下一个月的目标直接不问:那是纠缠不是关心。
- **不给写记忆的口子**(`allowsMemoryWrites: false`)。用户看不见「记住了…」那条气泡,也没法
  当场说一句"别记这个"。读照旧——不认识用户的话这一轮还不如不跑。
- 「跑过没跑过」拿**那条会话**当标记,不另开字段。会话就是这次跑出来的产物;多一个字段就多
  一处能和产物对不上的状态。通知正文也从那条会话读,不存第二份摘要。
- **`ChatSession.isDerived` 分开「他问过什么」和「我们替他问过什么」**。最要紧的一处是
  `InterestProfile`:后台替他查了三次体重不代表他关心体重,而那份统计反过来又会影响后台去查
  什么——不挡住就是自己喂自己。上一份没人接话的进展报告也靠它认出来,由新的顶掉;每周攒一段
  一年就是五十二段,而用户根本没看过前面那些。他一旦回了一句,那段就是真的对话,再也不顶。
- 目标进展在通知里**排在触发点前面、待跟进后面**。理由同「待跟进排在所有触发点前面」:那是
  用户自己定下的事,触发点只是数据里冒出来的现象。没跑出结论就整个不进通知——空口一句
  「你的『减脂』还在进行中」是零信息,不如把位置让给触发点。
- 点开回到哪条线由**排程时**写进 `userInfo.threadId`,不让收件方现算。通知说的是「减脂」、
  点开却落在 check-in,两边谁对谁错没人说得清。

## 会话目录的开销(`SessionIndexEntry` / `SessionStore`)

会话文件是这个 app 里**最大**的一类数据:一轮里两次健康查询的原始输出就有一万多字符,而
`storedTurn.exactTranscript` 会把同样的文本再存一份给模型回放用。而读目录这件事跑在用户已经
在等的时候——每轮回复结束刷一次列表、`search_sessions` 每次调用建一次索引、从 check-in 进 app
找一次延续线。

一年三百条会话,全量解一遍 **380ms / 峰值 +41MB**,而上面三件事各自解一遍。改成三条之后是
**33ms / +0.2MB**,增量刷新 **个位数毫秒**:

1. **只解用得上的键**(`SessionIndexEntry`)。`JSONDecoder` 不会去实例化没声明的字段,省掉的
   正是最大的那半边(`storedTurn`、每份 `HealthReport` 的逐小时序列)。
2. **一次扫描喂三个读者**。列表、召回、延续线、兴趣统计查的是同一份索引。
3. **按文件改动时间增量更新**。一轮回复结束只有一个文件变了,没道理把另外二百九十九条重解一遍。

几条不要破坏的:

- **索引里不留对话全文**。它常驻在 actor 里,而检索只用得上用户说过的话(`userText`,还带上限)
  和几个工具名。跟着把一年的工具输出留在内存里,是为了一个每轮最多用两次的功能付整段内存。
- **标题只有一份算法**(`SessionTitle.make`)。界面走 `ChatSession.title`(手里是整份会话),索引
  走 `SessionIndexEntry`(手里只有几个键)。各写一遍迟早漂,漂的结果是列表上一个名字、召回结果
  里另一个名字。
- **写完必须让缓存失效**。删掉的会话还能被模型引用出来,是这套东西最难解释的一种失灵。
- 读某一条的全文走 `load(id:)`,那是一次一条,该解就解——通知正文要模型真写的那句话,
  索引里没有。

`VanaTests/SessionStorePerformanceTests` 盯着耗时和 `phys_footprint` 峰值,改这块先看它。

## 架构:Siri(`Vana/Intents`)

四条 App Intent,分成两类,中间**没有第三种形态**:

- `TodayStatusIntent` / `LastNightSleepIntent` / `LastWorkoutIntent` 在后台跑,念
  `SpokenBrief` 本地算出来的一两句话。不联网、不看 API key。让 Siri 等一轮模型调用
  (联网 + 几轮工具 + 流式)只会超时,而超时之后用户听到的是"出了点问题"。
- `AskVanaIntent` 收一个问题参数,`.foreground(.immediate)` 把 app 拉起来,经
  `VanaLaunchRouter` → `CheckInLaunch(autoSend: true)` 进一条新会话,由完整的 agent 回答。
  Siri 和 check-in 通知共用 `openedCheckIn` 这一个入口;多一条路进 app 不该多一套载入逻辑。

几个容易踩的点:

- **不需要 Apple Intelligence**。App Shortcuts 是短语匹配,前提是句子里出现 app 名字
  (`\(.applicationName)`,解析成 Vana)。中国大陆没有 Apple Intelligence,这条路照样能用;
  反过来,"让 Siri 自由多轮代理式对话"那条现在做不了,别往那个方向设计。
- **短语按 app 的开发语言登记**。`project.yml` 里的 `developmentLanguage` 和
  `DEVELOPMENT_LANGUAGE` 都必须是 `zh-Hans`,留在 en 的话 `VanaShortcuts` 里写的中文在中文
  Siri 上一句都匹配不上。验证办法:clean build 的日志里应该有
  `Training '...' for zh-Hans`,产物里应该有 `Vana.app/Metadata.appintents/nlu`。
- **后台弹不出授权面板**,所以查之前先 `HealthStore.readAccess()` 问清楚,没授权就照实说
  "先打开 Vana"。锁屏时 HealthKit 整个库读不了(`isDatabaseLocked`),那是另一句话,不是
  "没有数据"。三种读不到的原因分开说,用户才知道下一步该干什么。
- `SpokenBrief` 里取数和组句是分开的:组句是纯函数,`now` / `calendar` 从外面传,
  `SpokenBriefTests` 盯着"昨晚是几天前"和"比平均多还是少"这两处。
- 触发点识别复用 `HealthSituation.detect()`,和 check-in 通知、首屏建议同一套。同一份数据
  在三个地方说出三种结论,用户只会觉得这 app 自己都没想清楚。

## HealthKit 授权(`HealthStore.requestAuthorizationIfNeeded`)

**要读的东西不在这台设备上,就不要去问。** 病历(FHIR)那两个 `HKClinicalType` 属于
Health Records,它按地区开放,设备和账号状态也算数——SDK 头文件里写得很直白:
"Call supportsHealthRecords before attempting to request authorization for any clinical types."
不问这一句就去申请,拿到的不是一个错误,而是**一句话都没有**:面板不弹,请求不回话,
`await` 停在那儿。

2026-08-21 那次审核就栽在这条上:iPad Air (M4) 上按设置页那颗「请求读取 Apple 健康」,
那一行一直停在「正在请求…」。同一台设备上启动时那次请求照常弹了面板——两次的差别只有一处,
设置页那次多带了病历那两类(`force` 才带)。

两条不要破坏的:

- **`supportsHealthRecords` 每次现问,不缓存**。文档明写它会随着账号在恢复、同步过程中变化。
  查不到病历时那份输出说的是「这台设备上没有『健康记录』功能,要看化验单请拍一张」,
  **不是**「没有找到记录,去『健康』App 里连接医院」——后者在这条路上是让用户白跑一趟。
  按老规矩,这不是错误(报成错误模型会以为工具坏了,换个说法再调一次)。
- **界面上不许有转不完的指示器**。设置页那颗按钮挂一条看门狗(12 秒),到点就停止显示
  「正在请求」并说明去「健康」App 里改。看门狗**不取消那次请求**(HealthKit 没有取消 API)。
  判断类型的那一步(`requestedTypes(force:supportsHealthRecords:)`)是纯函数,那个 Bool 从
  外面传进来:真机上的地区状态测试里造不出来。

**2026-08-25 同一颗按钮又被打回一次**,措辞变成「按了没有反应」。病历那条是对的,但它只是
三处里的一处——剩下两处都在 app 这一侧,而且都是**一个 await 永远不回话**的形状:

- **系统面板不能在另一层还在退场的时候 present**。首次那一屏(`DataUseNoticeSheet`)按完的
  那一帧,`hasAcceptedDataUseNotice` 就变了;挂在它的 onChange 上去请求授权,present 正好
  撞上还在进行的 dismiss——那次 present 悄悄地不发生,而 HealthKit 那个 await 跟着永远悬着。
  改挂在 `fullScreenCover(onDismiss:)` 上:退场动画真的结束之后才到,那时候这条链是空的。
  **第一次打开正好走这条路**,而审核员每次都是第一次打开。
- **这一侧根本不 await 那张面板**(`requestAuthorizationIfNeeded` → `present`)。这是这块
  东西的关键判断,推翻了原来那版和第一次修它的那版:`requestAuthorization` 回来的那个结果
  **本来就用不上**——iOS 从不告诉 app 读取权限被拒了(拒绝和「没有数据」长得一模一样),
  等它回来界面上能多说的只有一句「你的选择已保存」,而用户刚在面板上按完,他比谁都清楚。
  拿一句零信息的话去换「这颗按钮可能永远 disable 在正在请求上」,正是两次被拒的共同根源。
  面板扔出去就返回,防重复靠 `panelStaleAfter`(60 秒)而不是靠 await。

**两个上限,不能混成一个**(`statusTimeout` 5 秒 / `panelTimeout` 30 秒):

- **纯查询那几个**(`statusForAuthorizationRequest`、`supportsHealthRecords`)不弹任何 UI,
  没有一个正当理由让它花掉 5 秒,超了就是系统那边不回话。
- **面板那一段前面站着一个人**。给它设一个短上限,就是在用户读着面板的第 8 秒报一句
  「系统的授权面板没有响应」——指着他眼前那张面板说它不存在。第一版超时就是这么写的,
  修一个 blocking 顺手造了一个说谎的提示。**唯一能等的东西,是那个不需要人参与的东西。**
- 同理 `supportsHealthRecords()` 是同步 API 而且要读地区/账号状态,不在主线程上问
  (`HealthStore.supportsHealthRecords(_:)`),问不出来按 false 走。

**每一处 HealthKit 的 await 都要自己带一个头**(`HealthStore.withDeadline`),不只是那颗按钮:

- `requestOnDemand`(血压、化验单)跑在**用户正等着回复的时候**,这里悬住就是一条永远转
  下去的回复。它是唯一要等面板的一条(不等就必然查出空),所以用宽的那个上限;超时照常
  往下查,「血压没有数据」本来就是一句正常的话。
- `readAccess()` 跑在 Siri 的后台里,而 Siri 自己有超时:这一句悬住,用户听到的是
  「出了点问题」。问不出来按 `.ready` 走下去,让查询那一侧去分辨锁屏和没数据——报
  `.notRequested` 是让一个早就授权过的人白跑一趟。

**按完必定说一句话,而且没弹面板的那几次要挡在他面前**(`HealthAuthStatus.needsAttention`
→ alert)。面板真弹出来的那次不弹确认框(他刚在上面做完选择);没弹、失败、系统不回话这三种
里屏幕上其余部分什么都不变,只留一行灰色小字——那正好读成「按了没反应」。alert 里带一颗
「打开『健康』App」:已经决定过的项 iOS 不会再问,那儿是这条路上唯一能改的地方。

`VanaTests/HealthAuthorizationTests` 盯着这一套。

## 架构:合规(`Vana/Legal`)

上架要的那几样东西。它们看起来是"贴几段文字",实际上每一条都在声明 app 的行为,而**声明和
行为对不上就是这一整块的唯一失败模式**——而且是静默的:用户读不到 plist,那一页空白只有真的点
进去才看得见。`VanaTests/ComplianceTests` 盯着能自动检查的那部分。ASC 那半边(年龄分级、
隐私营养标签、审核备注)在 `APPSTORE.md`。

- **权限用途字符串必须和 app 实际做的事逐字对上**。端上模型撤掉之后,健康授权面板上一度还写着
  「端上模型模式下数据不离开设备」——在用户做决定的那一刻许一个已经做不到的诺,而那正是审核第一眼
  读到的地方。改这几句之前先想清楚现在到底发生了什么。
- **隐私说明只有一份**(`PrivacyPolicy.html`)。打进 app 包由 WKWebView 渲染,同一个文件原样发布当
  ASC 那个 URL。写成原生 SwiftUI 再另存一份 HTML 去发,就是同一份法律文本的两个副本,改一处忘一处
  是迟早的事,而对不上的那条恰好是审核在核对的。CSS 里字体**不要写进 `font:` 简写**:里面带
  `-apple-system` 时 WebKit 丢掉整条声明,整页汉字变成豆腐块(踩过)。
- **首次那一屏讲三件事:什么会发出去、什么不会、发给谁**(`DataUseNotice`),排在 HealthKit 授权面板
  **前面**——告知的意义全在于它发生在决定之前。不做成勾选式同意书:那个形状暗示在签协议,而实际发生
  的只是告知。正反两组都要具体到可以被验证(「照片原件不出这台设备,只发识别出来的文字」),
  「我们重视你的隐私」这种话写了等于没写。设置页「关于」里是同一份,不另写一遍。
- **急症规则排在系统提示第一条**,而且对家人成员一样发。它是唯一一条「先别答」的规则,排到第十条
  和没写没有区别。自伤那条单独一句(不是打 120,是危机热线和身边的人)。**第三条「不要滥用」和前
  两条一样要紧**:把每次疲劳都升级成急诊建议的助手,等于没有这条规则。
- **界面上要认得出 HealthKit**(`HealthKitAttribution`,Guideline 2.5.1)。用了 HealthKit 就得在
  界面上说得出来,而一颗心形图标加「你的健康数据」说不出**数据是从 Apple 的「健康」App 来的**
  ——2026-08-19 被判的正是这一条。一处定义,首屏欢迎卡、设置页那一节、状况详情页、每一张查询
  结果面板说的都是它;每一句同时报三件事:来源(Apple「健康」App / HealthKit)、只读、不写不改。
  中文用户认得的是「健康」App,审核员认得的是 HealthKit,两个名字都要在。家人成员那一屏不出——
  那条路上一个健康工具都没挂,写上就是许一个给不了的东西。
- **「AI 生成」一条会话只标一次**,挂在第一段回答下面。每条都挂,三句话之后它就是背景噪音了。
- **健康数据不进备份**(`TenantPaths.excludeFromBackup`,启动时跑)。`Documents/` 默认要进设备备份,
  而那份备份多数人存在 iCloud 上——会话文件里逐轮记着 HealthKit 的数值,附件里是带姓名和就诊号的
  化验单。代价是**换新手机时这些数据不跟着走**,隐私说明里照实写了这一条;盘上的行为和那份文件必须
  逐字对上(上一版正好写反了,有测试盯着)。标在目录上,底下新建的东西不用各自再标。
- **版本号两项由构建设置生成**(`MARKETING_VERSION` / `CURRENT_PROJECT_VERSION`)。
  `GENERATE_INFOPLIST_FILE` 开着时,写在基础 plist 里的那两行会被生成器盖掉,而没有
  `CFBundleShortVersionString` 的 build 传不上 ASC——本地跑起来一切正常,「关于」那行版本显示成一个
  破折号是唯一的迹象。
- **`NSHealthUpdateUsageDescription` 必须在所有配置里都有**,哪怕这个 app 一次都不请求写
  (唯一写健康数据的 `DebugSeeder` 整个是 `#if DEBUG`,正式构建里只剩
  `requestAuthorization(toShare: [], read:)`)。上传时的静态检查认的是**符号**,不看你传了个
  空集合——少了这一项 ASC 直接拒收(`Missing purpose string in Info.plist`),而 HealthKit 没有
  只读的授权 API,躲不掉。这一条和「声明一个从不申请的权限是白送审核一个问号」是冲突的,
  冲突里传不上去的那一边输。所以正式构建那句话的读者是审核员不是用户(那张面板永远不会弹),
  它要说的就是「只读、不写」这件事;Debug 那份单独覆盖,那边是真的会写。

## 默认 provider 和模型必须**落盘**(`EngineSettings.seedDefaultsIfNeeded`)

`@AppStorage(modelKey) = defaultModel` 只是「读不到时显示什么」,它一个字都不会写进
UserDefaults;而发请求那一侧读的是 `string(forKey:)`,拿到的是 nil。**2026-08-19 那次审核
就栽在这条缝里**:设置页上 Provider 写着 DeepSeek、模型写着 DeepSeek V4 Flash、key 也存好了,
一发消息却是「需要先在设置里选择云端模型」——而屏幕上没有任何一处能让他把这个看着已经选好的
模型再选一遍。审核员照着备注一步都没做错。

所以默认值只能有一份,而且必须是真的存下来的那一份:`VanaApp.init` 里补种子,发请求的三处
(聊天、后台派生、用药说明)和「看不看得了图」全部走 `EngineSettings.selection`。补种子
**只填空**——用户自己清空的模型照样留空(换到目录里没有内置模型的 provider 时会发生),
那时候拿 DeepSeek 的模型名去顶,就是拿这把钥匙去敲另一家的门,也就是 2026-08-16 被拒的那次。

配套的两条同样重要,它们管的是**这件事再发生时用户看到什么**:

- **`refreshEngineAvailability` 连模型一起问**。只问 key 的话,模型那格空着的人照样发得出去,
  换来一个错误气泡;问了就走「把设置页推到他面前」那条已经有的路。
- **报错气泡底下那颗按钮分两种**(`ChatViewModel.recovery(for:)`):配不齐、或者 key 没通过
  验证时是「去设置」,别的才是「重试」。审核员按了两次重试,屏幕上就是两条一模一样的报错——
  这两类失败重试一百次都是同一句话。判据现算,他配完回来那颗按钮自己就变回「重试」。

`VanaTests/CloudSetupTests` 盯着这一套。

## 架构:多语言(`Localizable.xcstrings` / `InfoPlist.xcstrings`)

**中文是源语言,英文是加上去的那一份。** `developmentLanguage: zh-Hans` 不动(Siri 短语按它
登记),键就是中文原文——`Text("昨晚睡得怎么样？")` 本来就是 `LocalizedStringKey`,界面代码
一个字都不用改写成 key,英文那份在 catalog 里。

- **`Text("a" + "b")` 走的是逐字那条重载**,`String` 拼接不是字面量,编译器抽不到,英文那边
  永远是中文。跨行拼接一律合并成一条多行字面量(`\` 续行),排版一个字没变,而它现在是
  `LocalizedStringKey`。**新写的每一处都要照这条来。**
- **界面用的纯 `String` 包进 `String(localized:)`**:首屏那句话(`HealthSituation` /
  `HealthVitals`)、设置页的状态提示、通知文案、用药/记忆/成员那些枚举标签。**发给模型的
  prompt 一个字不动**——那一份的读者是模型,不是用户。
- **组句时分隔号、句号、首字母大小写都跟着语言走**(`HealthSituation.sentence`)。中文那句是
  「A；B。」,英文是「A; B.」,而英文还要把第一个字母大写:每一条 `brief` 都是半句话
  (「no sleep recorded last night」),而它们各自都可能排在最前面。
- **别在句子中间拼方向词**。「比基线\(多/少) 3 次」在英文里语序整个不一样,拼出来的那半句
  没法翻——多和少各写一条整句(`HealthVitals.comparison`)。
- **回答的语言跟着界面走**(`HealthAssistantInstructions.replyLanguage`,读
  `Bundle.main.preferredLocalizations`)。界面全英文、回答却是中文,是这个 app 里最扎眼的一处
  不一致,而用户没有任何地方能改它。急救号码同理:英文那份不点名 120,只说「当地急救电话」。
- **权限用途字符串走 `InfoPlist.xcstrings`**。那几句是用户按「允许」之前读到的最后一段字,
  漏一条,英文用户在那一刻读到的是中文——而审核员正是从那一屏开始看这个 app 的。有测试盯着。
- **隐私说明两份**(`Vana/Legal/<语言>.lproj/PrivacyPolicy.html`),app 内跟着界面语言走,
  `site/build.sh` 把它们发到 `/privacy/` 和 `/privacy/en/`。**改一份就要改另一份**——对不上的
  那条恰好是审核在核对的。英文那份也有测试盯着必须说到的几件事。
- **测试 scheme 钉在 zh-Hans**(`project.yml` 的 `scheme.language`)。模拟器默认是英文系统,
  而合规、首屏、Siri 那几套断言盯的是**中文原文**(它们要挡的是「声明和行为对不上」,那是
  逐字的)。不钉住的话,一加英文全挂,而挂的原因和代码没有关系。

新加/改动界面文案之后,把新键收进来:

```bash
xcodebuild -exportLocalizations -project Vana.xcodeproj -localizationPath /tmp/loc -exportLanguage en
```

这一步会把编译器抽到的键写进 `Localizable.xcstrings`(空的 `localizations`),再补英文。
**Xcode 那个"自动填充"只在 IDE 里跑,`xcodebuild build` 不会填**——所以少了这一步,新写的
中文在英文设备上就是原样显示,而本机开发时看不出来。

**故意没做的**:Siri 短语和 `SpokenBrief` 那几句口播还是中文(它们按开发语言登记,而英文
Siri 那条路要另验一遍);站点的介绍页和支持页也还是中文。

## 依赖

云端 LLM 请求走 AIKit(`zjywill/aikitswift`),以本地包 `../aikitswift` 引入——构建这个项目需要旁边有 aikitswift 的 checkout。`AgentRuntime` 是仓库内的本地包,没有外部依赖。

## 约定

- iOS 26 only,不写旧版本可用性分支。
- **辅助调用一律显式 `thinking: .off`**(首屏建议、追问 chip、抽记忆——凡是"让模型写几行短句"
  或者"从文本里挑几条"的)。`CallOptions.thinking` **留空不等于关**:那是接受模型的默认,而
  DeepSeek、Qwen、GLM、Gemini Flash 的默认是思考。代价不只是慢——思考算进 output,一个
  `maxOutputTokens: 120` 的请求会在写出正文之前用光预算,然后解析不到东西整体作废,界面上
  只表现为"没生成"。`.off` 对不支持思考的 provider 是安全的(AIKit 那边会连同 warning 一起
  丢掉),不需要先查 `supportsReasoning`——要查的是反过来发 `.on` 的时候。
- HealthKit 只读;API key 只进 Keychain。
- 工具只返回按天(或按晚)聚合值,不返回原始样本。逐小时序列只画在结果面板里,不进模型上下文。
